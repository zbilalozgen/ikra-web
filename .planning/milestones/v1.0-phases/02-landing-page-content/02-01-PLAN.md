---
phase: 02-landing-page-content
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - components.json
  - components/ui/button.tsx
  - components/ui/card.tsx
  - lib/utils.ts
  - public/screenshots/01-home-screen.png
  - public/screenshots/02-home-full.png
  - public/screenshots/03-deeplink-verse.png
  - public/screenshots/04-push-notification.png
  - public/watermark-glyph.png
autonomous: true
requirements:
  - LAND-03
  - LAND-04
user_setup: []

must_haves:
  truths:
    - "shadcn/ui is initialized for Tailwind v4 with button + card primitives importable from @/components/ui/ [covers D-16]"
    - "lucide-react is installed and resolvable for downstream feature icons [covers D-12]"
    - "Four iOS screenshots exist in public/screenshots/ so Hero (LAND-03) and Showcase (LAND-03) can <Image>-render them [covers D-10, D-14, D-20]"
    - "Watermark glyph exists at public/watermark-glyph.png for Hero background decoration [covers D-18]"
    - "Project still builds clean (npm run build exit 0) after deps and asset copy"
  artifacts:
    - path: "components.json"
      provides: "shadcn config (style, RSC, Tailwind v4 mode, aliases)"
      contains: "components"
    - path: "components/ui/button.tsx"
      provides: "shadcn Button primitive (imported as @/components/ui/button)"
      exports: ["Button", "buttonVariants"]
    - path: "components/ui/card.tsx"
      provides: "shadcn Card primitive used by Features section"
      exports: ["Card", "CardHeader", "CardContent"]
    - path: "lib/utils.ts"
      provides: "cn() helper used by shadcn primitives"
      exports: ["cn"]
    - path: "public/screenshots/01-home-screen.png"
      provides: "Showcase tile 1"
    - path: "public/screenshots/02-home-full.png"
      provides: "Hero phone image + Showcase tile 2"
    - path: "public/screenshots/03-deeplink-verse.png"
      provides: "Showcase tile 3"
    - path: "public/screenshots/04-push-notification.png"
      provides: "Showcase tile 4"
    - path: "public/watermark-glyph.png"
      provides: "Hero watermark background"
  key_links:
    - from: "components/ui/card.tsx"
      to: "lib/utils.ts"
      via: "import { cn } from '@/lib/utils'"
      pattern: "from [\"']@/lib/utils[\"']"
    - from: "package.json"
      to: "lucide-react"
      via: "dependencies"
      pattern: "\"lucide-react\""
---

<objective>
Stand up the foundation Plan 02-02 needs to build all four landing sections: install and initialize shadcn/ui (Tailwind v4 mode, button + card only), install lucide-react, and copy the iOS screenshots + watermark glyph from the wip-muslim mobile app repo into `public/`.

Purpose: Plan 02-02 builds Hero / Features / Showcase / Stats and `<Image>`-renders five static assets. Those assets and shadcn primitives must already exist at file-system level before that plan runs, otherwise its build will fail on missing imports and missing public files. This plan has no visible page change — `app/page.tsx` is untouched here.

Output: shadcn initialized (`components.json`, `lib/utils.ts`, `components/ui/button.tsx`, `components/ui/card.tsx`), `lucide-react` in `package.json`, and 5 image files in `public/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page-content/02-CONTEXT.md
@.planning/phases/02-landing-page-content/02-UI-SPEC.md
@.planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-deployable-skeleton/01-UI-SPEC.md
@app/globals.css
@app/layout.tsx
@components/Footer.tsx
@package.json

<interfaces>
<!-- Existing project shape Plan 02-01 must integrate with cleanly. -->

From package.json (Phase 1):
- dependencies: next 16.2.6, react 19.2.4, react-dom 19.2.4
- devDependencies: @tailwindcss/postcss ^4, tailwindcss ^4, typescript ^5, eslint 9
- No tailwind.config.js exists (Tailwind v4 @theme in app/globals.css only) — shadcn init MUST run in Tailwind v4 mode

From app/globals.css (Phase 1, @theme tokens already shipped):
- --color-emerald-{50..900}, --color-gold-{50..900}, --color-navy-{50..900}
- --color-cream, --color-surface-card, --color-surface-card-hover, --color-surface-warm
- --spacing-{xs,sm,md,lg,xl,2xl,3xl,screen,section,touch}
- --duration-fast: 150ms
- --font-sans wired to var(--font-inter)
- shadcn primitives MUST consume these tokens via existing utility class names; do NOT introduce a separate shadcn token theme

From tsconfig.json (Phase 1):
- "@/*" path alias already configured → shadcn aliases default to @/components and @/lib

Asset sources (existing files outside this repo, verified to exist on disk):
- /Users/zbilalozgen/repos/wip-muslim/analysis/screenshots/ios/01-home-screen.png
- /Users/zbilalozgen/repos/wip-muslim/analysis/screenshots/ios/02-home-full.png
- /Users/zbilalozgen/repos/wip-muslim/analysis/screenshots/ios/03-deeplink-verse.png
- /Users/zbilalozgen/repos/wip-muslim/analysis/screenshots/ios/04-push-notification.png
- /Users/zbilalozgen/repos/wip-muslim/src/assets/images/watermark-glyph.png
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui (Tailwind v4) and add button + card primitives</name>
  <read_first>
    - @package.json (current deps — confirm Tailwind v4, no tailwind.config.js)
    - @app/globals.css (existing @theme tokens — shadcn must not duplicate or override these)
    - @.planning/phases/02-landing-page-content/02-UI-SPEC.md §Design System (Tool row locks shadcn Tailwind v4 mode, button + card only)
    - @.planning/phases/02-landing-page-content/02-CONTEXT.md D-16 (shadcn init, button + card only; no other primitives)
    - @tsconfig.json (verify @/* alias)
  </read_first>
  <action>
    Per D-16: initialize shadcn/ui in Tailwind v4 mode, then add only the `button` and `card` primitives.

    Run from repo root with non-interactive flags:
      `npx shadcn@latest init -d -y`
    The `-d` flag selects defaults (style: new-york, base color: neutral, css variables: yes, RSC: yes, alias `@/components`, alias `@/lib/utils`). The `-y` flag auto-confirms overwrites. shadcn will auto-detect Tailwind v4 from `@tailwindcss/postcss` in devDependencies and `@import "tailwindcss"` in `app/globals.css`.

    Expected files created by init:
      - `components.json` (shadcn config — committed)
      - `lib/utils.ts` (cn helper — committed)
      - Potential additions to `app/globals.css` (shadcn injects its own CSS variable layer — KEEP the existing @theme brand tokens intact; do NOT delete or reorder Phase 1 tokens)

    Then add the two primitives non-interactively:
      `npx shadcn@latest add button card -y`

    Expected files created: `components/ui/button.tsx`, `components/ui/card.tsx`.

    Do NOT add any other shadcn components (no dialog, no input, no sheet) — D-16 locks scope to button + card only.

    If shadcn init prompts despite `-y` (e.g. asks about overwriting `globals.css`), accept the overwrite ONLY if you can then re-merge the Phase 1 @theme block back in. The Phase 1 brand tokens (`--color-emerald-*`, `--color-gold-*`, `--color-navy-*`, `--color-cream`, `--color-surface-*`, `--spacing-*`, `--duration-fast`, `--font-sans`) MUST remain in `app/globals.css` after this task — they are the source of truth for the rest of the phase. shadcn's own variables (e.g. `--background`, `--foreground`, `--radius`) may coexist below the existing @theme block.

    After install: `npm run build` MUST exit 0. The skeleton home page (Phase 1 `app/page.tsx`) is untouched and must still render with brand colors.

    Do NOT touch `app/page.tsx`, `app/layout.tsx`, `components/Footer.tsx`, or the stub route pages.
  </action>
  <verify>
    <automated>test -f components.json && test -f lib/utils.ts && test -f components/ui/button.tsx && test -f components/ui/card.tsx && grep -q "from [\"']@/lib/utils[\"']" components/ui/card.tsx && grep -q "from [\"']@/lib/utils[\"']" components/ui/button.tsx && grep -E "color-emerald-600.*#006B3F" app/globals.css && grep -q "spacing-section" app/globals.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists and is committed
    - `lib/utils.ts` exists, exports `cn`
    - `components/ui/button.tsx` exists, exports `Button` and `buttonVariants`, imports `cn` from `@/lib/utils`
    - `components/ui/card.tsx` exists, exports at minimum `Card` (CardHeader, CardContent, CardTitle, CardDescription as provided by shadcn add are also fine), imports `cn` from `@/lib/utils`
    - No other files added to `components/ui/` (no dialog/input/sheet/etc.)
    - `app/globals.css` still contains the Phase 1 @theme block with `--color-emerald-600: #006B3F`, `--color-gold-500: #D4A843`, `--color-navy-800: #1B2A4A`, `--color-cream: #F2F2F7`, `--spacing-section: 64px`, `--duration-fast: 150ms`, `--font-sans` wired to Inter
    - `app/page.tsx`, `app/layout.tsx`, `components/Footer.tsx`, and all stub route files are byte-identical to before this task
    - `npm run build` exits 0
    - No `tailwind.config.js` or `tailwind.config.ts` created (Tailwind v4 mode confirmed)
  </acceptance_criteria>
  <done>shadcn initialized in Tailwind v4 mode with button + card primitives importable as `@/components/ui/button` and `@/components/ui/card`; Phase 1 @theme tokens preserved; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Install lucide-react and copy iOS screenshots + watermark glyph into public/</name>
  <read_first>
    - @.planning/phases/02-landing-page-content/02-CONTEXT.md D-12 (Lucide icon mapping), D-18 (watermark source path), D-20 (Next.js Image with explicit width/height, CLS=0)
    - @.planning/phases/02-landing-page-content/02-UI-SPEC.md §Image Contract (5 source-to-public copy table) and §Design System (Lucide React row)
    - @package.json (current dep set — confirm lucide-react absent before install)
  </read_first>
  <action>
    Two parts:

    Part A — Install lucide-react (D-12):
      Run from repo root: `npm install lucide-react`
      lucide-react is the icon library the Features section will import (`Smartphone`, `Volume2`, `Stars`, `Compass`, `Share2`, `WifiOff`, `LayoutDashboard`). Lock it as a `dependencies` entry in `package.json` (not devDependencies) since icons render in the React tree at runtime.

    Part B — Copy assets per D-18 + UI-SPEC §Image Contract:

      Source files (verified to exist on disk):
        - `/Users/zbilalozgen/repos/wip-muslim/analysis/screenshots/ios/01-home-screen.png`
        - `/Users/zbilalozgen/repos/wip-muslim/analysis/screenshots/ios/02-home-full.png`
        - `/Users/zbilalozgen/repos/wip-muslim/analysis/screenshots/ios/03-deeplink-verse.png`
        - `/Users/zbilalozgen/repos/wip-muslim/analysis/screenshots/ios/04-push-notification.png`
        - `/Users/zbilalozgen/repos/wip-muslim/src/assets/images/watermark-glyph.png`

      Destination paths in this repo (create `public/screenshots/` directory first):
        - `public/screenshots/01-home-screen.png`
        - `public/screenshots/02-home-full.png`
        - `public/screenshots/03-deeplink-verse.png`
        - `public/screenshots/04-push-notification.png`
        - `public/watermark-glyph.png`

      Use `cp` (preserves bytes); do NOT re-encode or resize. The UI-SPEC declares explicit `width`/`height` for Next.js `<Image>` props (Hero 280×560, Watermark 320×320, Showcase tiles 240×480) — the Next.js image CDN handles responsive scaling at request time, so source aspect ratio is the only constraint.

    Do NOT touch `app/page.tsx`, `app/layout.tsx`, `app/globals.css`, `components/Footer.tsx`, or anything in `components/ui/` from Task 1.

    Final check: `npm run build` MUST still exit 0 (no consumer yet imports these — Plan 02-02 wires them).
  </action>
  <verify>
    <automated>grep -q "\"lucide-react\"" package.json && test -d public/screenshots && test -f public/screenshots/01-home-screen.png && test -f public/screenshots/02-home-full.png && test -f public/screenshots/03-deeplink-verse.png && test -f public/screenshots/04-push-notification.png && test -f public/watermark-glyph.png && cmp public/screenshots/02-home-full.png /Users/zbilalozgen/repos/wip-muslim/analysis/screenshots/ios/02-home-full.png && cmp public/watermark-glyph.png /Users/zbilalozgen/repos/wip-muslim/src/assets/images/watermark-glyph.png && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains `"lucide-react"` (any caret-pinned version)
    - `node_modules/lucide-react/package.json` exists (install actually ran)
    - `public/screenshots/` directory exists
    - All 5 image files exist at their declared destinations
    - All 4 screenshots are byte-identical to the wip-muslim source files (verified by `cmp`)
    - Watermark glyph is byte-identical to source
    - No re-encoded / resized variants — bytes match originals
    - No other files added to `public/` beyond the 5 declared assets and the existing Phase 1 contents
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>lucide-react resolvable for Plan 02-02's feature tiles; 5 image assets present in `public/` ready for `<Image>` consumers; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | Adds `shadcn` (transitively via npx) + `lucide-react` as new runtime deps |
| local filesystem (wip-muslim) → public/ | Reads PNG assets from a sibling repo and stages them as public static files served by Vercel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | npm install (lucide-react, shadcn/ui deps) | mitigate | Both packages are first-party-published with very high download counts and long histories; pin via standard caret in package.json; verified through RESEARCH/CONTEXT phase decision D-16 (shadcn official registry only) and D-12 (lucide-react explicit). No `[ASSUMED]` or `[SUS]` per Phase 2 context. |
| T-02-01-02 | Tampering | Asset copy from wip-muslim repo | mitigate | Copy only the 5 named files listed in UI-SPEC §Image Contract; verify byte-equality with `cmp` against source before committing; never copy wildcards or unverified files |
| T-02-01-03 | Information Disclosure | Static assets in public/ | accept | All copied assets are app marketing screenshots intended for public marketing use — no PII, no internal data |
| T-02-01-04 | Tampering | shadcn init mutating app/globals.css | mitigate | Acceptance criterion explicitly verifies Phase 1 brand tokens remain in globals.css after init; if init removes them, task fails and must be redone with merge |
</threat_model>

<verification>
After both tasks:
- `npm run build` exits 0
- `git status` shows clean diff containing only: `components.json`, `lib/utils.ts`, `components/ui/button.tsx`, `components/ui/card.tsx`, 5 PNG files under `public/`, and changes to `package.json` + `package-lock.json` (and possibly `app/globals.css` if shadcn appended its variable layer — Phase 1 tokens preserved)
- Visiting `/` in dev/prod still renders the Phase 1 skeleton (no visual change yet — this is intentional foundation prep)
</verification>

<success_criteria>
- shadcn/ui initialized in Tailwind v4 mode with button + card primitives importable from `@/components/ui/`
- `lucide-react` installed as a runtime dependency
- 5 PNG assets present in `public/` at the paths the UI-SPEC §Image Contract declares
- Phase 1 brand `@theme` tokens in `app/globals.css` preserved verbatim
- `npm run build` exits 0
- Plan 02-02 can begin with zero external setup
</success_criteria>

<output>
Create `.planning/phases/02-landing-page-content/02-01-SUMMARY.md` documenting:
- shadcn version installed, files created under `components/ui/`
- lucide-react version
- 5 assets copied (source → destination, byte-equal confirmation)
- Build status
- Confirmation Phase 1 @theme block intact
</output>
