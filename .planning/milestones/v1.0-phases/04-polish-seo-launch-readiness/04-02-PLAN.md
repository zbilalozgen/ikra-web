---
phase: 04-polish-seo-launch-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/opengraph-image.tsx
autonomous: false
requirements:
  - SEO-02
must_haves:
  truths:
    - "Pages render correct Open Graph / Twitter share cards with brand imagery when shared (SEO-02) [covers D-38]"
    - "The OG image looks great in iMessage, WhatsApp, Twitter/X previews — emerald background, white İkra wordmark, gold tagline, watermark glyph"
    - "Single OG image shared across home + Privacy + Terms + Support routes (per D-38 v1 scope)"
  artifacts:
    - path: "app/opengraph-image.tsx"
      provides: "Dynamic 1200×630 brand-accurate Open Graph image via next/og ImageResponse"
      exports: ["default", "alt", "size", "contentType"]
      contains: "ImageResponse"
  key_links:
    - from: "app/opengraph-image.tsx"
      to: "public/watermark-glyph.png"
      via: "fetched via new URL('/watermark-glyph.png', import.meta.url) and read as Buffer or read from disk via fs"
      pattern: "watermark-glyph"
    - from: "app/opengraph-image.tsx default export"
      to: "Next.js file convention auto-route /opengraph-image"
      via: "App Router file-system routing for opengraph-image.{js,ts,tsx}"
      pattern: "export default.*ImageResponse|return new ImageResponse"
---

<objective>
Produce a single, dynamic Open Graph image at `/opengraph-image` that renders a 1200×630 brand card: emerald-600 background, white "İkra" wordmark, cream tagline, watermark glyph at low opacity in the corner. This image is auto-discovered by Next.js for every route — and 04-01's `metadata.openGraph.images` + `metadata.twitter.images` explicitly reference it for determinism.

Purpose: The image side of SEO-02. Without this, share previews fall back to a thumbnail of the page or nothing at all — looks unprofessional pre-launch.

Output: One new file `app/opengraph-image.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-seo-launch-readiness/04-CONTEXT.md

<interfaces>
<!-- next/og ImageResponse contract (Next.js 16). -->

```typescript
import { ImageResponse } from "next/og";

export const alt = "İkra — Quran Verses & Sahih Hadith Widget App";
export const size = { width: 1200, height: 630 };
export const contentType = "image/png";

export default async function Image() {
  return new ImageResponse(
    (<div style={{ /* inline styles only */ }}>...</div>),
    { ...size, fonts?: [{ name, data, weight, style }] }
  );
}
```

Constraints:
- Only inline `style` objects work. NO Tailwind class names (the runtime is Edge Satori SVG, not the browser).
- All assets must be fetched at runtime via `fetch(new URL("./asset.png", import.meta.url))` and converted to ArrayBuffer, then passed as `<img src={`data:image/png;base64,${...}`}/>` or directly as ArrayBuffer to a custom font. Use `fetch` rather than `fs` for Edge compatibility.
- Inter font is NOT bundled. For v1, the system fallback inside Satori is acceptable; or fetch the font Google Fonts CSS provides and pass via `fonts` parameter.

Brand tokens (inline values — Satori has no @theme):
- emerald-600 background: `#006B3F`
- cream text accent: `#F2F2F7`
- gold-500 (optional accent line): `#D4A843`
- White wordmark: `#FFFFFF`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create app/opengraph-image.tsx producing a 1200×630 brand card</name>
  <files>app/opengraph-image.tsx</files>
  <behavior>
    - Exports `alt = "İkra — Quran Verses & Sahih Hadith Widget App"`, `size = { width: 1200, height: 630 }`, `contentType = "image/png"`.
    - Default export is an async function returning `new ImageResponse(...)`.
    - Rendered JSX root is a `<div>` with `width: 100%`, `height: 100%`, `display: flex`, `flexDirection: column`, `alignItems: center`, `justifyContent: center`, `backgroundColor: "#006B3F"`, `position: relative`.
    - Centered "İkra" wordmark: `fontSize: 144`, `fontWeight: 700`, `color: "#FFFFFF"`, letter-spacing tightened slightly (`letterSpacing: -2`).
    - Tagline directly below: text `"Quran Verses & Sahih Hadith Widget App"`, `fontSize: 36`, `color: "#F2F2F7"`, `marginTop: 24`.
    - Watermark glyph (from `public/watermark-glyph.png`) absolute-positioned in the top-right corner at ~`opacity: 0.18`, width 320, height 320, with `top: 0`, `right: 0`.
    - At runtime, fetching `/opengraph-image` returns HTTP 200 with `content-type: image/png` and a binary body roughly 50–250 KB.
  </behavior>
  <action>
    Create `app/opengraph-image.tsx`. Import `ImageResponse` from `next/og`. Export the three runtime constants `alt`, `size`, `contentType` (per D-38 + the Next.js file convention). Default-export an `async function Image()` that builds the JSX tree described in `<behavior>` and returns `new ImageResponse(jsx, { ...size })`.

    For the watermark glyph: at the top of the function, `const watermark = await fetch(new URL("../public/watermark-glyph.png", import.meta.url)).then(r =&gt; r.arrayBuffer());` — then render as `<img src={watermark as unknown as string} ... />` (Satori accepts ArrayBuffer here despite the type) OR base64-encode and use `data:image/png;base64,...` (more portable; prefer this). Use `Buffer.from(watermark).toString("base64")` for the encoding.

    All styling is inline `style={{ ... }}` objects (D-CLAUDE: Satori has no class support). Use the exact hex values from `<interfaces>`: emerald `#006B3F`, white `#FFFFFF`, cream `#F2F2F7`. Font: omit a custom font for v1 — system Satori fallback is acceptable per "Claude's Discretion". If the wordmark feels too thin, optionally fetch Inter Bold from `https://fonts.googleapis.com/...` and pass via `fonts: [{ name: "Inter", data: fontBuffer, weight: 700, style: "normal" }]` to `ImageResponse` options; otherwise skip. Document the choice in SUMMARY.

    Do NOT add `runtime = "edge"` directive — Next 16 defaults to the Node runtime for `next/og` and `fetch` works either way; let Next pick. Do NOT introduce dark-mode logic (D-04). Do NOT use Tailwind class names (Satori cannot parse them).
  </action>
  <verify>
    <automated>cd /Users/zbilalozgen/repos/ikra-web &amp;&amp; npx --no-install next build 2&gt;&amp;1 | tail -30 &amp;&amp; grep -E "ImageResponse|export.*alt|export.*size|export.*contentType" app/opengraph-image.tsx | grep -v '^//' | wc -l | awk '$1 &gt;= 4 { print "PASS: required exports present"; exit 0 } { print "FAIL: missing required exports"; exit 1 }'</automated>
  </verify>
  <done>app/opengraph-image.tsx exists with all four required exports; `npx next build` completes without error; the `/opengraph-image` route appears in the build output route list.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dynamic Open Graph image at `/opengraph-image` rendering the İkra brand card (emerald background, white wordmark, cream tagline, watermark glyph corner).</what-built>
  <how-to-verify>
    1. Run `npx next dev` (or `npx next start` after a build) and open `http://localhost:3000/opengraph-image` in a browser.
    2. Confirm the image is exactly 1200 × 630 (browser dev tools → Network tab → Headers, or right-click → "Open image in new tab" and inspect dimensions).
    3. Visual check: emerald background reads correctly (not too dark, not blue-tinted), "İkra" wordmark is white and crisp, tagline reads in cream below, watermark glyph is visible-but-subtle in a corner.
    4. Drop the localhost URL into the [Meta Sharing Debugger preview tool](https://www.opengraph.xyz/) OR `curl -I http://localhost:3000/opengraph-image` — content-type must be `image/png`, content-length within ~50–250 KB.
    5. If the wordmark looks too light/thin (system fallback is geometric sans), optionally accept and ship, or ask the executor to wire a Google Fonts Inter Bold load.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe rendering issues (font, alignment, color, glyph) for fixes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → runtime asset fetch | OG image route fetches local public/ asset via import.meta.url — bundled at build, served from the deployment. No external/untrusted source. |
| social platform → OG image URL | Twitter/Meta crawlers fetch /opengraph-image. Read-only; no input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Denial of service | /opengraph-image dynamic route | accept | Next.js caches the response; social crawlers fetch infrequently. Vercel edge caches further. No DoS surface beyond static asset serving. |
| T-04-05 | Tampering | Watermark glyph asset | accept | Source asset shipped from repo; build-time fingerprint via Vercel deploy hash. No mutable storage. |
</threat_model>

<verification>
- Build emits `/opengraph-image` route.
- File exports alt/size/contentType plus default async function.
- Human verifies visual rendering (Task 2 checkpoint).
</verification>

<success_criteria>
- SEO-02 image side satisfied.
- Image is 1200×630 PNG with brand colors and wordmark.
- No JS animation library introduced.
- No dark-mode utilities.
</success_criteria>

<output>
Create `.planning/phases/04-polish-seo-launch-readiness/04-02-SUMMARY.md` when done. Record any font decision (system fallback vs. Inter Bold) and the rendered file size.
</output>
