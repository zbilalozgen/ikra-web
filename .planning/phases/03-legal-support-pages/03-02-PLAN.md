---
phase: 03-legal-support-pages
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/privacy/page.tsx
autonomous: true
requirements:
  - LEGAL-01
user_setup: []

must_haves:
  truths:
    - "Visitor at /privacy sees H1 'Privacy Policy', a 'Last updated: June 1, 2026' subtitle, a Table of Contents card, and 9 H2 sections in the locked order from D-22 [covers D-21, D-22, LEGAL-01]"
    - "The 9 H2 section titles appear in this exact order: Overview, Data we collect (and what we don't), Third-party services, Local storage on your device, Children's privacy, Your rights, Data retention, Changes to this policy, Contact us [covers D-22]"
    - "Each H2 has an `id` attribute matching the TOC anchor href (e.g. <h2 id='data-we-collect'>) so TOC links scroll to the section without JS [covers D-28]"
    - "Third-party services section names the actual mobile-app data processors: Firebase (Analytics + Crashlytics + Remote Config), RevenueCat, Apple/Google IAP, Expo Notifications, Expo Tracking Transparency, AppsFlyer, Google Mobile Ads — and explicitly states no user accounts and no PII collected by the website [covers D-21]"
    - "Page exports a Next.js metadata object with title 'Privacy Policy — İkra' and a 1-sentence description [covers D-29]"
    - "Content uses the in-place stub file path app/privacy/page.tsx — route, footer hrefs, and layout are unchanged from Phase 1 [covers D-26]"
    - "Body wrapper uses `<article className=\"prose ... max-w-3xl mx-auto px-screen py-section\">` with inline prose-* overrides mapping H2 to emerald-600, body to navy-800, links to gold-500 with hover emerald-600 [covers D-26, D-27]"
    - "TOC is a static `<nav aria-label=\"On this page\">` containing an `<ol>` of anchor links to the 9 H2 ids, styled with bg-surface-card, border-navy-800/10, rounded-lg, p-md, text-sm [covers D-28]"
    - "Bottom cross-link strip links to /terms and /support and `← Back to home` (/), separated by gold-500 middle-dot dividers, centered, with mt-2xl pt-lg border-t border-navy-800/10 [covers D-30]"
    - "Page is a server component — no `\"use client\"`, no `useState`, no event handlers"
    - "Site stays light-mode only — zero `dark:`, zero `prefers-color-scheme`, zero `prose-invert`, zero literal `duration-150`"
    - "npm run build exits 0; /privacy renders as a static (○) route with the full policy body"
  artifacts:
    - path: "app/privacy/page.tsx"
      provides: "Privacy Policy page — H1, last-updated, TOC, 9 H2 sections per D-22, contact email, cross-links"
      min_lines: 120
      exports: ["default", "metadata"]
      contains: "Privacy Policy"
  key_links:
    - from: "app/privacy/page.tsx"
      to: "Next.js metadata system"
      via: "export const metadata"
      pattern: "export const metadata"
    - from: "app/privacy/page.tsx"
      to: "support@ikraapp.com"
      via: "mailto: anchor in Contact us section"
      pattern: "mailto:support@ikraapp.com"
    - from: "app/privacy/page.tsx"
      to: "/terms"
      via: "next/link in cross-link strip"
      pattern: "href=[\"']/terms[\"']"
    - from: "app/privacy/page.tsx"
      to: "/support"
      via: "next/link in cross-link strip"
      pattern: "href=[\"']/support[\"']"
    - from: "app/privacy/page.tsx"
      to: "/"
      via: "next/link Back to home"
      pattern: "href=[\"']/[\"']"
    - from: "app/privacy/page.tsx"
      to: "@tailwindcss/typography"
      via: "prose utility class"
      pattern: "className=[\"'][^\"']*\\bprose\\b"
---

<objective>
Replace the Phase 1 stub body of `app/privacy/page.tsx` with the real, store-submission-ready Privacy Policy required by Apple App Store and Google Play. The page is hand-written, app-specific, and discloses the actual mobile app's data flows (Firebase, RevenueCat, Expo Notifications, Expo Tracking Transparency, AppsFlyer, Google Mobile Ads, Apple/Google IAP) — NOT a generic template. Covers LEGAL-01.

Purpose: Without this page, İkra cannot submit to either app store — the Privacy Policy URL is a hard submission requirement. D-21 / D-22 lock the policy as app-specific, with a 9-section structure, anchored TOC, last-updated stamp, and contact email. D-26 / D-27 / D-28 / D-29 / D-30 lock the layout, typography, TOC styling, metadata, and cross-link footer strip.

Output: A single edited file (`app/privacy/page.tsx`) that renders the complete policy at `/privacy`. No new files, no new components, no new routes — Phase 1 wired everything; this plan fills the body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-legal-support-pages/03-CONTEXT.md
@.planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-deployable-skeleton/01-UI-SPEC.md
@.planning/phases/02-landing-page-content/02-CONTEXT.md
@app/globals.css
@app/layout.tsx
@app/privacy/page.tsx
@components/Footer.tsx
@CLAUDE.md

<interfaces>
<!-- Operational handles the executor needs verbatim. -->

Mobile-app data-flow source-of-truth (from /Users/zbilalozgen/repos/wip-muslim/package.json — DO NOT re-discover; these are the actual dependencies that the Privacy Policy MUST disclose under D-21):

| Dependency | What it does | Disclose as |
|------------|--------------|-------------|
| @react-native-firebase/analytics | Anonymous app usage analytics | "Firebase Analytics — anonymous app usage events; no PII" |
| @react-native-firebase/crashlytics | Crash reports | "Firebase Crashlytics — anonymous crash diagnostics" |
| @react-native-firebase/remote-config | Server-driven feature flags | "Firebase Remote Config — receives feature toggles; sends device-level metadata only" |
| react-native-purchases (RevenueCat) | Subscription IAP management | "RevenueCat — anonymized subscriber ID for restoring purchases across devices" |
| expo-notifications | Local + push verse reminders | "Expo Notifications — local and push notifications for opt-in verse reminders; APNs/FCM device token" |
| expo-tracking-transparency | iOS ATT prompt | "Apple App Tracking Transparency — iOS prompts the user; we use the user's choice to govern downstream attribution SDKs" |
| react-native-appsflyer | Install attribution (mobile app attribution) | "AppsFlyer — mobile install attribution; uses device IDs only when ATT consent is granted on iOS" |
| react-native-google-mobile-ads | AdMob (free tier ads) | "Google AdMob — serves ads in the free tier; premium subscription removes ads. Personalized ads only when ATT consent is granted on iOS" |
| Apple StoreKit / Google Play Billing | Subscription receipts | "Apple App Store / Google Play Billing — handles all subscription payments; we never see or store payment-card details" |

Note: 03-CONTEXT D-21 mentions "no advertising SDK detected" — but `react-native-google-mobile-ads` and `react-native-appsflyer` ARE in the mobile app's package.json (verified at plan time). The Privacy Policy MUST disclose them. CONTEXT's "no ad SDK" line is superseded by the verified package.json — disclose AdMob and AppsFlyer plainly.

Website-vs-app distinction (CRITICAL for the "Overview" and "Data we collect" sections):
- The WEBSITE (ikra-web.vercel.app) is a static marketing site with no first-party cookies, no analytics, no forms, and no user accounts.
- The APP (İkra mobile) is what most of the policy describes, because the policy URL is required for app-store submission.
- The policy MUST disclose this split clearly so a reader understands which surface each disclosure applies to.

Locked H2 section order + ids (from D-22; ids are kebab-case slugs of the visible title):

| Order | H2 Title | id (anchor slug) |
|-------|----------|------------------|
| 1 | Overview | `overview` |
| 2 | Data we collect (and what we don't) | `data-we-collect` |
| 3 | Third-party services | `third-party-services` |
| 4 | Local storage on your device | `local-storage` |
| 5 | Children's privacy | `childrens-privacy` |
| 6 | Your rights | `your-rights` |
| 7 | Data retention | `data-retention` |
| 8 | Changes to this policy | `changes-to-this-policy` |
| 9 | Contact us | `contact-us` |

Locked literal strings (verbatim, no paraphrasing):
- Page H1: `Privacy Policy`
- Last-updated line: `Last updated: June 1, 2026`
- TOC label (`aria-label` AND visible label): `aria-label="On this page"`, visible label string `On this page`
- Contact email: `support@ikraapp.com` (D-25)
- Metadata title: `Privacy Policy — İkra` (em dash U+2014; already present in current stub — preserve)
- Metadata description (1 sentence): write one similar to: `How İkra collects, uses, and protects your data — covering the iOS and Android app, this website, and third-party services.`

Body wrapper className (inline prose-* overrides per D-27 Claude's Discretion):
```
prose prose-headings:text-emerald-600 prose-headings:font-semibold prose-p:text-navy-800 prose-li:text-navy-800 prose-a:text-gold-500 prose-a:no-underline hover:prose-a:underline prose-a:underline-offset-4 hover:prose-a:text-emerald-600 max-w-3xl mx-auto px-screen py-section
```

Outer `<main>` wrapper: NONE — `app/layout.tsx` already supplies `<main className="flex-1">`. The page's top-level element should be `<article>` directly. (Note: Phase 1 stub uses `<section>` — replace, do not nest inside an extra `<main>`.)

TOC styling (D-28) — applied to the `<nav>` element, NOT inside the prose wrapper (to avoid prose's `<ol>` default styles from competing with the card chrome). Place the `<nav>` BEFORE the prose content but AFTER the H1+last-updated. Use `not-prose` to opt the nav out of prose styling:
```
not-prose bg-surface-card border border-navy-800/10 rounded-lg p-md text-sm mt-lg
```
The TOC `<ol>` should be a simple decimal list. Use `text-navy-800 hover:text-emerald-600 transition-colors duration-fast` on each `<a>`.

Cross-link strip at bottom (D-30) — placed OUTSIDE the prose `<article>` but inside the page return so it sits below the policy body. Use `not-prose` is unnecessary since it's outside the article. Structure:
```
<nav aria-label="Related pages" className="max-w-3xl mx-auto px-screen mt-2xl pt-lg border-t border-navy-800/10 text-sm text-navy-800/70 flex flex-wrap items-center justify-center gap-sm">
  <Link href="/terms">Terms of Service</Link>
  <span aria-hidden="true" className="text-gold-500">·</span>
  <Link href="/support">Support</Link>
  <span aria-hidden="true" className="text-gold-500">·</span>
  <Link href="/">← Back to home</Link>
</nav>
```
Each Link should carry `hover:text-emerald-600 transition-colors duration-fast`.

Imports needed:
- `import type { Metadata } from "next";` (already in stub — keep)
- `import Link from "next/link";` (for cross-link strip and TOC anchors — but TOC anchors can be plain `<a href="#anchor">` since they're same-page; use plain `<a>` for TOC, `<Link>` for cross-page nav)

Server component: do NOT add `"use client"`. No state, no effects, no event handlers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write Privacy Policy body in app/privacy/page.tsx</name>
  <read_first>
    - @app/privacy/page.tsx (current Phase 1 stub — full body to be replaced; the existing metadata export structure is reusable)
    - @.planning/phases/03-legal-support-pages/03-CONTEXT.md (D-21 through D-30 + Specific Ideas — tone, definite voice, "We collect X. We do not collect Y.")
    - @.planning/phases/01-foundation-deployable-skeleton/01-UI-SPEC.md (typography roles, spacing scale, color tokens inherited from Phase 1)
    - @.planning/phases/02-landing-page-content/02-CONTEXT.md D-26 (max-w-3xl mx-auto px-screen py-section pattern)
    - @components/Footer.tsx (brand-tone analog — reverent voice; cross-link styling reference for hover transitions)
    - @app/layout.tsx (confirms `<main className="flex-1">` already wraps page content — do NOT add another `<main>`)
    - @app/globals.css (confirms `@plugin "@tailwindcss/typography"` registered in Plan 03-01)
  </read_first>
  <action>
    Replace the entire body of `app/privacy/page.tsx` (delete the existing `<section>...This page is under construction...</section>` and all its centering classes; preserve the file structure with `metadata` export + default-exported component).

    Keep the existing imports/exports skeleton:
      - `import type { Metadata } from "next";` (KEEP)
      - Add: `import Link from "next/link";`
      - `export const metadata: Metadata = { title: "Privacy Policy — İkra", description: "<one sentence>" };` — KEEP the title (already correct from stub), ADD a 1-sentence `description` per D-29 (suggested: `How İkra collects, uses, and protects your data — covering the iOS and Android app, this website, and third-party services.`)
      - `export default function PrivacyPage()` returning JSX (replace the stub return entirely)

    Top-level JSX structure (the function returns a React fragment containing the article + cross-link strip):

      ```
      <>
        <article className="prose prose-headings:text-emerald-600 prose-headings:font-semibold prose-p:text-navy-800 prose-li:text-navy-800 prose-a:text-gold-500 prose-a:no-underline hover:prose-a:underline prose-a:underline-offset-4 hover:prose-a:text-emerald-600 max-w-3xl mx-auto px-screen py-section">
          <h1>Privacy Policy</h1>
          <p className="not-prose text-sm text-navy-800/60 mt-xs">Last updated: June 1, 2026</p>

          {/* Table of Contents */}
          <nav aria-label="On this page" className="not-prose bg-surface-card border border-navy-800/10 rounded-lg p-md text-sm mt-lg">
            <p className="font-semibold text-navy-800 mb-sm">On this page</p>
            <ol className="list-decimal list-inside flex flex-col gap-xs">
              <li><a href="#overview" className="text-navy-800 hover:text-emerald-600 transition-colors duration-fast">Overview</a></li>
              ... (9 entries total, ids in the table above)
            </ol>
          </nav>

          {/* 9 H2 sections — each with matching id */}
          <h2 id="overview">Overview</h2>
          <p>...</p>

          <h2 id="data-we-collect">Data we collect (and what we don't)</h2>
          <p>...</p>

          ... (sections 3-9) ...

          <h2 id="contact-us">Contact us</h2>
          <p>... <a href="mailto:support@ikraapp.com">support@ikraapp.com</a> ...</p>
        </article>

        {/* Cross-link strip (D-30) */}
        <nav aria-label="Related pages" className="max-w-3xl mx-auto px-screen mt-2xl pt-lg border-t border-navy-800/10 text-sm text-navy-800/70 flex flex-wrap items-center justify-center gap-sm">
          <Link href="/terms" className="hover:text-emerald-600 transition-colors duration-fast">Terms of Service</Link>
          <span aria-hidden="true" className="text-gold-500">·</span>
          <Link href="/support" className="hover:text-emerald-600 transition-colors duration-fast">Support</Link>
          <span aria-hidden="true" className="text-gold-500">·</span>
          <Link href="/" className="hover:text-emerald-600 transition-colors duration-fast">← Back to home</Link>
        </nav>
      </>
      ```

    Section-by-section content requirements (executor writes the prose body; each section MUST cover the bullet points below; tone is reverent, scholarly, plain English — NOT legalese; use definite voice "We collect X. We do not collect Y." per CONTEXT Specific Ideas):

    Section 1 — Overview (`id="overview"`):
      - MUST state that this Privacy Policy covers BOTH (a) the İkra mobile app on iOS and Android, AND (b) the marketing website at ikra-web.vercel.app (or current production URL).
      - MUST state the operator: "the developer of İkra" (or similar honest framing — no fictional company entity).
      - MUST state the high-level posture: no user accounts, no PII collection, anonymous device-level usage data only.
      - 2-4 sentences. Reverent, calm tone.

    Section 2 — Data we collect (and what we don't) (`id="data-we-collect"`):
      - MUST list, as a `<ul>`, what IS collected: anonymous Firebase Analytics events, Firebase Crashlytics crash reports, Firebase Remote Config device-level metadata, anonymized RevenueCat subscriber ID (only if the user makes a purchase), APNs/FCM device token (only if the user opts in to verse reminders), AppsFlyer install-attribution device ID (only when ATT consent is granted on iOS).
      - MUST list, as a `<ul>` immediately after, what is NOT collected: no name, no email address, no phone number, no postal address, no precise location, no contacts, no photos, no health data, no payment-card details (Apple/Google handle billing). State explicitly: the WEBSITE collects nothing — no cookies, no analytics, no forms.
      - Use the format: "We collect: ... We do not collect: ..." (definite voice).

    Section 3 — Third-party services (`id="third-party-services"`):
      - MUST list each of these third-party processors with a one-line description of what they do for the app (see the data-flow table in `<interfaces>` above for the verbatim disclosure phrasing):
        * Firebase (Analytics + Crashlytics + Remote Config) — operated by Google LLC
        * RevenueCat — operated by RevenueCat, Inc.
        * Apple App Store / Google Play Billing — operated by Apple Inc. / Google LLC
        * Expo Notifications — operated by Expo / EAS
        * Apple App Tracking Transparency — iOS framework
        * AppsFlyer — operated by AppsFlyer Ltd.
        * Google AdMob — operated by Google LLC
      - Use a `<ul>` with one `<li>` per processor.
      - MUST mention that AdMob personalized ads and AppsFlyer attribution only operate when the user grants ATT consent on iOS (no equivalent OS-level prompt on Android — defaults follow Google Play Services standard behavior).
      - MUST state that paid (premium) subscribers do not see AdMob ads.

    Section 4 — Local storage on your device (`id="local-storage"`):
      - MUST disclose that the app stores Quran text, hadith text, audio cache, preferences, and bookmarks LOCALLY on the device (via Expo SQLite + Expo Secure Store).
      - MUST state this local data never leaves the device and is removed when the user uninstalls the app.
      - 2-3 sentences.

    Section 5 — Children's privacy (`id="childrens-privacy"`):
      - MUST state the app is not directed at children under 13.
      - MUST state we do not knowingly collect data from children under 13.
      - MUST state that if a parent believes their child has been collected from, they can contact `support@ikraapp.com` and we will remove any data we hold.
      - 2-3 sentences.

    Section 6 — Your rights (`id="your-rights"`):
      - MUST mention EU/UK GDPR briefly (right to access, rectify, erase, restrict, port, object).
      - MUST mention California CCPA briefly (right to know, delete, opt-out of sale; clarify that we do not sell personal information).
      - MUST direct readers to email `support@ikraapp.com` to exercise any of these rights.
      - 3-5 sentences across 1-2 paragraphs.

    Section 7 — Data retention (`id="data-retention"`):
      - MUST state Firebase Analytics retention follows Google's default (currently 14 months for event-level data) and is anonymous.
      - MUST state Crashlytics retention follows Firebase's default.
      - MUST state RevenueCat retains subscriber records only as long as the subscription is active plus any period required by Apple/Google billing.
      - 3-4 sentences.

    Section 8 — Changes to this policy (`id="changes-to-this-policy"`):
      - MUST state we may update this policy and that the "Last updated" date at the top reflects the most recent change.
      - MUST state material changes will be communicated via the app or app-store release notes.
      - 2 sentences.

    Section 9 — Contact us (`id="contact-us"`):
      - MUST contain a paragraph with an active `mailto:` link: `<a href="mailto:support@ikraapp.com">support@ikraapp.com</a>`.
      - MUST direct readers to use this address for privacy questions, data-rights requests, or any concerns.
      - 1-3 sentences.

    Tone notes (CONTEXT Specifics):
      - Reverent and factual. NOT preachy. NOT corporate.
      - Avoid "We may collect..." — be definite.
      - Plain English, no legalese padding.
      - No emoji.
      - No exclamation marks.
      - Use the `İ` (U+0130 Turkish capital I with dot above) consistently when spelling "İkra".

    Verbatim-string constraints:
      - H1: `Privacy Policy` (exact)
      - Last-updated line: `Last updated: June 1, 2026` (exact, with the comma)
      - TOC visible label: `On this page` (exact)
      - Email address: `support@ikraapp.com` (exact — D-25)
      - Cross-link labels: `Terms of Service`, `Support`, `← Back to home` (the arrow is U+2190 LEFTWARDS ARROW; if executor cannot type U+2190, fall back to ASCII `<-` and a follow-up edit will replace)
      - All 9 H2 titles MUST match the table in `<interfaces>` verbatim including the apostrophe in "Children's privacy" and the parenthetical "(and what we don't)" in section 2

    Do NOT add a section background — body inherits `bg-cream` from `app/layout.tsx`.
    Do NOT add an extra `<main>` wrapper.
    Do NOT add `"use client"`.
    Do NOT add `dark:` utilities.
    Do NOT use `prose-invert` or `prose-dark`.
    Do NOT use literal `duration-150`.
    Do NOT add Open Graph or Twitter meta — Phase 4 owns that.
    Do NOT add a "Print" button or other client-side controls.
    Do NOT add fictional company names, fictional addresses, or fictional DPO contacts.
  </action>
  <verify>
    <automated>test -f app/privacy/page.tsx && grep -q "export const metadata" app/privacy/page.tsx && grep -F "Privacy Policy — İkra" app/privacy/page.tsx && grep -F "Last updated: June 1, 2026" app/privacy/page.tsx && grep -F "On this page" app/privacy/page.tsx && grep -q 'aria-label="On this page"' app/privacy/page.tsx && grep -q 'id="overview"' app/privacy/page.tsx && grep -q 'id="data-we-collect"' app/privacy/page.tsx && grep -q 'id="third-party-services"' app/privacy/page.tsx && grep -q 'id="local-storage"' app/privacy/page.tsx && grep -q 'id="childrens-privacy"' app/privacy/page.tsx && grep -q 'id="your-rights"' app/privacy/page.tsx && grep -q 'id="data-retention"' app/privacy/page.tsx && grep -q 'id="changes-to-this-policy"' app/privacy/page.tsx && grep -q 'id="contact-us"' app/privacy/page.tsx && grep -q "href=\"#overview\"" app/privacy/page.tsx && grep -q "href=\"#contact-us\"" app/privacy/page.tsx && grep -F "Data we collect (and what we don't)" app/privacy/page.tsx && grep -F "Third-party services" app/privacy/page.tsx && grep -F "Local storage on your device" app/privacy/page.tsx && grep -F "Children's privacy" app/privacy/page.tsx && grep -F "Your rights" app/privacy/page.tsx && grep -F "Data retention" app/privacy/page.tsx && grep -F "Changes to this policy" app/privacy/page.tsx && grep -F "Contact us" app/privacy/page.tsx && grep -q "mailto:support@ikraapp.com" app/privacy/page.tsx && grep -q "Firebase" app/privacy/page.tsx && grep -q "RevenueCat" app/privacy/page.tsx && grep -q "Expo Notifications" app/privacy/page.tsx && grep -q "AdMob" app/privacy/page.tsx && grep -q "AppsFlyer" app/privacy/page.tsx && grep -q "Tracking Transparency" app/privacy/page.tsx && grep -q "max-w-3xl" app/privacy/page.tsx && grep -q "px-screen" app/privacy/page.tsx && grep -q "py-section" app/privacy/page.tsx && grep -E "className=\"[^\"]*\\bprose\\b" app/privacy/page.tsx && grep -q "prose-headings:text-emerald-600" app/privacy/page.tsx && grep -q "prose-a:text-gold-500" app/privacy/page.tsx && grep -q "bg-surface-card" app/privacy/page.tsx && grep -q "border-navy-800/10" app/privacy/page.tsx && grep -q "rounded-lg" app/privacy/page.tsx && grep -q "href=\"/terms\"" app/privacy/page.tsx && grep -q "href=\"/support\"" app/privacy/page.tsx && grep -q "href=\"/\"" app/privacy/page.tsx && grep -q "border-t border-navy-800/10" app/privacy/page.tsx && grep -q "text-gold-500" app/privacy/page.tsx && grep -q "duration-fast" app/privacy/page.tsx && ! grep -q "This page is under construction" app/privacy/page.tsx && ! grep -q "use client" app/privacy/page.tsx && ! grep -E "dark:|prefers-color-scheme|prose-invert|prose-dark" app/privacy/page.tsx && ! grep -q "duration-150" app/privacy/page.tsx && [ $(grep -c "^<h2\|<h2 id=" app/privacy/page.tsx) -ge 9 ] && npm run build 2>&1 | tee /tmp/ikra-03-02-build.log | grep -E "Compiled successfully|Route \(app\)" && ! grep -E "Error|Failed to compile" /tmp/ikra-03-02-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `app/privacy/page.tsx` contains an `export const metadata` with `title: "Privacy Policy — İkra"` and a 1-sentence `description`
    - File contains H1 string `Privacy Policy` and last-updated `Last updated: June 1, 2026`
    - TOC `<nav aria-label="On this page">` present with visible label `On this page`
    - All 9 H2 titles present verbatim including apostrophe in `Children's privacy` and the parenthetical in `Data we collect (and what we don't)`
    - All 9 H2 elements carry the locked `id` values: `overview`, `data-we-collect`, `third-party-services`, `local-storage`, `childrens-privacy`, `your-rights`, `data-retention`, `changes-to-this-policy`, `contact-us`
    - TOC contains anchor links `href="#overview"` through `href="#contact-us"` matching the H2 ids
    - Third-party services section names all of: Firebase, RevenueCat, Expo Notifications, Tracking Transparency, AppsFlyer, AdMob
    - Contact section contains `<a href="mailto:support@ikraapp.com">`
    - Body wrapper uses `prose ... max-w-3xl mx-auto px-screen py-section` with inline `prose-headings:text-emerald-600` and `prose-a:text-gold-500` overrides
    - TOC styled with `bg-surface-card border border-navy-800/10 rounded-lg p-md`
    - Cross-link strip at bottom links to `/terms`, `/support`, `/` with gold-500 middle-dot dividers and `border-t border-navy-800/10 mt-2xl pt-lg`
    - No `"use client"`, no `dark:`, no `prefers-color-scheme`, no `prose-invert`, no `duration-150`
    - No "under construction" placeholder copy
    - `npm run build` exits 0; `/privacy` renders without error
  </acceptance_criteria>
  <done>`app/privacy/page.tsx` ships a complete, hand-written, app-specific Privacy Policy with TOC, 9 sections matching D-22, accurate disclosure of the mobile app's actual data flows (Firebase, RevenueCat, Expo Notifications, Tracking Transparency, AppsFlyer, AdMob, Apple/Google Billing), mailto contact, and the cross-link strip. Covers LEGAL-01. App-store Privacy Policy URL requirement is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static page | Pure static render; no user input, no API surface |
| visitor → mailto: link | Hands off to the OS mail client; no server-side handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | Public disclosure of actual third-party processors | accept | This IS the policy's purpose — disclosure of Firebase, RevenueCat, AppsFlyer, AdMob, etc. is required by App Store / Play Store policy. No internal-only secret is leaked; all processors are inferable from the public app binary anyway. |
| T-03-02-02 | Repudiation | "Last updated: June 1, 2026" manually maintained | accept | CONTEXT Deferred Ideas explicitly defers git-time-driven last-updated to v2. The manual string is the authoritative date for v1; Plan 03-XX edits in future will update it as needed. |
| T-03-02-03 | Spoofing | mailto:support@ikraapp.com hardcoded | mitigate | D-25 locks this address; executor MUST use it verbatim. Developer responsibility to confirm the mailbox routes before app-store submission (see CONTEXT D-25 follow-up note). |
| T-03-02-04 | Tampering | Hand-written legal text (no auto-generation) | mitigate | Executor writes prose; checker verifies coverage of D-22 sections + mobile-app data flows. Acceptance criteria enumerate required identifiers (Firebase / RevenueCat / etc.) — checker greps for presence. |
| T-03-02-05 | Information Disclosure | TOC anchor ids leak internal URL fragment structure | accept | Standard practice; TOC fragments are user-facing nav, not secrets. |
</threat_model>

<verification>
Plan-level checks:
- `/privacy` renders the full policy with H1 + last-updated + TOC + 9 H2 sections + cross-link strip
- All 9 H2 `id`s match the 9 TOC `href`s exactly
- All third-party processors from the mobile app's package.json are named in section 3
- `npm run build` exits 0; `/privacy` listed as static (○) route
- `git diff` shows changes scoped to `app/privacy/page.tsx` only
- No dark-mode utilities, no `prose-invert`, no `duration-150`
- File renders to >120 lines (substantive policy, not a stub)
</verification>

<success_criteria>
- LEGAL-01 satisfied: visitor can open `/privacy` and read a complete, store-submission-ready Privacy Policy
- Policy is hand-written and app-specific (NOT a template)
- 9-section structure per D-22 in the locked order with anchored TOC
- Brand-aligned typography via `prose` + inline `prose-*` overrides (D-27)
- Cross-link footer strip connects /privacy → /terms, /support, / (D-30)
- Light-mode only; no dark utilities introduced
- Build remains green
- App Store + Google Play Privacy Policy URL requirement met
</success_criteria>

<output>
Create `.planning/phases/03-legal-support-pages/03-02-SUMMARY.md` documenting:
- The single content task with its commit hash
- Confirmation of all 9 H2 sections + matching `id`s + TOC anchors
- List of disclosed third-party processors (must include: Firebase / RevenueCat / Apple+Google Billing / Expo Notifications / Tracking Transparency / AppsFlyer / AdMob)
- Confirmation that `npm run build` exits 0 and `/privacy` is a static (○) route
- Decision-coverage confirmation: D-21, D-22, D-26, D-27 (use-site half), D-28, D-29, D-30 all honored, with line-number pointers
- LEGAL-01 satisfaction note
- Any verbatim-copy deviations (should be zero)
</output>
</content>
</invoke>