---
phase: 03-legal-support-pages
plan: "04"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/support/page.tsx
autonomous: true
requirements:
  - LEGAL-03
user_setup: []

must_haves:
  truths:
    - "Visitor at /support sees H1 'Support', a primary mailto button-link to support@ikraapp.com, and an H2 'Frequently asked' section with 5 Q+A entries [covers D-24, D-25, LEGAL-03]"
    - "The 5 FAQ questions appear verbatim from D-24 in this order: 'How do I install the iOS / Android widget?', 'Audio isn't playing — what should I check?', 'How do I restore my premium purchase?', 'Daily verse notifications aren't arriving — how do I fix that?', 'How do I report a bug or request a feature?' [covers D-24]"
    - "Each FAQ question is wrapped in `<h3>` and each answer is wrapped in `<p>` — no `<details>` element, no JS, answers render open by default so they are indexed by search engines [covers D-24]"
    - "Primary contact button-link is styled prominently (emerald-600 background, white text, py-md px-lg, rounded-lg, min-h-touch) and uses `<a href=\"mailto:support@ikraapp.com\">` [covers D-24, D-25]"
    - "Page exports a Next.js metadata object with title 'Support — İkra' and a 1-sentence description [covers D-29]"
    - "Content uses the in-place stub file path app/support/page.tsx — route, footer hrefs, and layout are unchanged from Phase 1 [covers D-26]"
    - "Body wrapper uses `<article className=\"prose ... max-w-3xl mx-auto px-screen py-section\">` with inline prose-* overrides per D-27 [covers D-26, D-27]"
    - "Bottom cross-link strip links to /privacy and /terms and `← Back to home` (/), separated by gold-500 middle-dot dividers, centered, with mt-2xl pt-lg border-t border-navy-800/10 [covers D-30]"
    - "Page also displays a 'Last updated: June 1, 2026' subtitle below H1 for consistency with the legal pages [covers D-29]"
    - "Page is a server component — no `\"use client\"`, no state, no event handlers"
    - "Site stays light-mode only — zero `dark:`, zero `prefers-color-scheme`, zero `prose-invert`, zero literal `duration-150`"
    - "npm run build exits 0; /support renders as a static (○) route"
  artifacts:
    - path: "app/support/page.tsx"
      provides: "Support page — H1, mailto button-link, 5 FAQ Q+A entries per D-24, cross-links"
      min_lines: 90
      exports: ["default", "metadata"]
      contains: "Frequently asked"
  key_links:
    - from: "app/support/page.tsx"
      to: "Next.js metadata system"
      via: "export const metadata"
      pattern: "export const metadata"
    - from: "app/support/page.tsx"
      to: "support@ikraapp.com"
      via: "mailto: anchor in primary contact button-link AND in FAQ answers where appropriate"
      pattern: "mailto:support@ikraapp.com"
    - from: "app/support/page.tsx"
      to: "/privacy"
      via: "next/link in cross-link strip"
      pattern: "href=[\"']/privacy[\"']"
    - from: "app/support/page.tsx"
      to: "/terms"
      via: "next/link in cross-link strip"
      pattern: "href=[\"']/terms[\"']"
    - from: "app/support/page.tsx"
      to: "/"
      via: "next/link Back to home"
      pattern: "href=[\"']/[\"']"
    - from: "app/support/page.tsx"
      to: "@tailwindcss/typography"
      via: "prose utility class"
      pattern: "className=[\"'][^\"']*\\bprose\\b"
---

<objective>
Replace the Phase 1 stub body of `app/support/page.tsx` with the real Support page: a prominent `mailto:` contact button-link plus a 5-question FAQ that addresses the most common İkra app questions (widget install, audio issues, purchase restore, notifications, bug reports). Covers LEGAL-03 (App Store / Play Store submission requires a way for users to reach support).

Purpose: Apple and Google both require a published support URL at app submission. D-24 locks the structure: H1 "Support", primary `mailto:` styled as a button-link, and an H2 "Frequently asked" with 5 specific questions (verbatim) rendered as `<h3>` + `<p>` pairs (NOT `<details>`) so search engines index every answer. D-25 locks the email. D-26 / D-27 / D-29 / D-30 lock the layout, typography, metadata, and cross-link footer strip — identical pattern to the legal pages so the three pages feel like a cohesive set.

Output: A single edited file (`app/support/page.tsx`) that renders the support page at `/support`. No new files, no new components, no new routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-legal-support-pages/03-CONTEXT.md
@.planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-deployable-skeleton/01-UI-SPEC.md
@.planning/phases/02-landing-page-content/02-CONTEXT.md
@app/globals.css
@app/layout.tsx
@app/support/page.tsx
@components/Footer.tsx
@CLAUDE.md

<interfaces>
<!-- Operational handles the executor needs verbatim. -->

Locked literal strings (verbatim, no paraphrasing):
- Page H1: `Support`
- Last-updated line: `Last updated: June 1, 2026`
- H2 for FAQ: `Frequently asked`
- Contact email: `support@ikraapp.com` (D-25)
- Metadata title: `Support — İkra` (em dash U+2014; already present in current stub — preserve)
- Metadata description (1 sentence): suggested: `Get help with the İkra app — widget install, audio playback, purchase restore, notifications, and bug reports.`

Locked FAQ questions (D-24 — verbatim, in this exact order):

1. `How do I install the iOS / Android widget?`
2. `Audio isn't playing — what should I check?`  (note: em dash U+2014, lowercase apostrophe-t in "isn't")
3. `How do I restore my premium purchase?`
4. `Daily verse notifications aren't arriving — how do I fix that?`  (em dash, apostrophe)
5. `How do I report a bug or request a feature?`

Body wrapper className (inline prose-* overrides per D-27 — identical to legal pages):
```
prose prose-headings:text-emerald-600 prose-headings:font-semibold prose-p:text-navy-800 prose-li:text-navy-800 prose-a:text-gold-500 prose-a:no-underline hover:prose-a:underline prose-a:underline-offset-4 hover:prose-a:text-emerald-600 max-w-3xl mx-auto px-screen py-section
```

Primary mailto button-link (D-24 calls for a "button-link" — styled as a prominent button, but technically an `<a>` element with `mailto:` href). Use `not-prose` so the button's brand styling is not interfered with by `prose` defaults. Suggested classes:
```
not-prose inline-flex items-center justify-center bg-emerald-600 text-white font-semibold px-lg py-md rounded-lg min-h-touch hover:bg-emerald-700 transition-colors duration-fast no-underline mt-md
```
Label text: `Email us at support@ikraapp.com` (or shorter `Contact support@ikraapp.com`). Use `Email us at support@ikraapp.com` so the email address is visible in the button label itself (useful for accessibility and for users who want to copy the address without clicking).

FAQ entry structure (D-24):
- Each Q is `<h3>` (NOT `<h2>`, NOT `<details><summary>`, NOT a clickable accordion)
- Each A is `<p>` immediately following the `<h3>`
- All answers render open by default (no JS) so search engines index them
- Tone: concise, reverent, friendly — like a developer wrote it, not a corporate support team

Cross-link strip at bottom (D-30) — links to the OTHER 2 legal pages (`/privacy`, `/terms`) plus Back to home:
```
<nav aria-label="Related pages" className="max-w-3xl mx-auto px-screen mt-2xl pt-lg border-t border-navy-800/10 text-sm text-navy-800/70 flex flex-wrap items-center justify-center gap-sm">
  <Link href="/privacy">Privacy Policy</Link>
  <span aria-hidden="true" className="text-gold-500">·</span>
  <Link href="/terms">Terms of Service</Link>
  <span aria-hidden="true" className="text-gold-500">·</span>
  <Link href="/">← Back to home</Link>
</nav>
```
Each Link should carry `hover:text-emerald-600 transition-colors duration-fast`.

Imports needed:
- `import type { Metadata } from "next";` (already in stub — keep)
- `import Link from "next/link";` (for cross-link strip)

Server component: do NOT add `"use client"`. No state, no effects, no event handlers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write Support page body in app/support/page.tsx</name>
  <read_first>
    - @app/support/page.tsx (current Phase 1 stub — full body to be replaced; existing metadata export structure is reusable)
    - @.planning/phases/03-legal-support-pages/03-CONTEXT.md (D-24, D-25, D-26, D-27, D-29, D-30 + Specific Ideas + tone)
    - @.planning/phases/01-foundation-deployable-skeleton/01-UI-SPEC.md (typography roles, spacing scale, color tokens)
    - @.planning/phases/02-landing-page-content/02-CONTEXT.md D-26 (max-w-3xl mx-auto px-screen py-section pattern)
    - @components/Footer.tsx (brand-tone analog; cross-link styling reference)
    - @app/layout.tsx (confirms `<main className="flex-1">` already wraps page content)
    - @app/globals.css (confirms `@plugin "@tailwindcss/typography"` registered in Plan 03-01)
  </read_first>
  <action>
    Replace the entire body of `app/support/page.tsx` (delete the existing `<section>...This page is under construction...</section>` placeholder; preserve the file structure with `metadata` export + default-exported component).

    Keep / add imports:
      - `import type { Metadata } from "next";` (KEEP)
      - Add: `import Link from "next/link";`
      - `export const metadata: Metadata = { title: "Support — İkra", description: "<one sentence>" };` — KEEP the title (already correct from stub), ADD a 1-sentence `description` per D-29 (suggested: `Get help with the İkra app — widget install, audio playback, purchase restore, notifications, and bug reports.`)
      - `export default function SupportPage()` returning JSX (replace the stub return entirely)

    Top-level JSX structure (the function returns a Fragment containing the article + cross-link strip):

      ```
      <>
        <article className="prose prose-headings:text-emerald-600 prose-headings:font-semibold prose-p:text-navy-800 prose-li:text-navy-800 prose-a:text-gold-500 prose-a:no-underline hover:prose-a:underline prose-a:underline-offset-4 hover:prose-a:text-emerald-600 max-w-3xl mx-auto px-screen py-section">
          <h1>Support</h1>
          <p className="not-prose text-sm text-navy-800/60 mt-xs">Last updated: June 1, 2026</p>

          {/* Intro paragraph */}
          <p>
            (1-2 sentence reverent intro — see content requirements below)
          </p>

          {/* Primary mailto button-link */}
          <a
            href="mailto:support@ikraapp.com"
            className="not-prose inline-flex items-center justify-center bg-emerald-600 text-white font-semibold px-lg py-md rounded-lg min-h-touch hover:bg-emerald-700 transition-colors duration-fast no-underline mt-md"
          >
            Email us at support@ikraapp.com
          </a>

          {/* FAQ */}
          <h2>Frequently asked</h2>

          <h3>How do I install the iOS / Android widget?</h3>
          <p>(answer)</p>

          <h3>Audio isn't playing — what should I check?</h3>
          <p>(answer)</p>

          <h3>How do I restore my premium purchase?</h3>
          <p>(answer)</p>

          <h3>Daily verse notifications aren't arriving — how do I fix that?</h3>
          <p>(answer)</p>

          <h3>How do I report a bug or request a feature?</h3>
          <p>(answer)</p>
        </article>

        {/* Cross-link strip (D-30) */}
        <nav aria-label="Related pages" className="max-w-3xl mx-auto px-screen mt-2xl pt-lg border-t border-navy-800/10 text-sm text-navy-800/70 flex flex-wrap items-center justify-center gap-sm">
          <Link href="/privacy" className="hover:text-emerald-600 transition-colors duration-fast">Privacy Policy</Link>
          <span aria-hidden="true" className="text-gold-500">·</span>
          <Link href="/terms" className="hover:text-emerald-600 transition-colors duration-fast">Terms of Service</Link>
          <span aria-hidden="true" className="text-gold-500">·</span>
          <Link href="/" className="hover:text-emerald-600 transition-colors duration-fast">← Back to home</Link>
        </nav>
      </>
      ```

    Content requirements:

    Intro paragraph (right after H1 + last-updated, before the mailto button):
      - 1-2 sentences. Reverent, warm, brief.
      - MUST state the simplest path to help is to email the support address.
      - MUST hint that the FAQ below covers the most common questions.
      - Suggested phrasing: `We are a small team behind İkra and we read every message. If you have a question, run into an issue, or have a suggestion, please email us — answers to the most common questions are below.`

    FAQ Answer 1 — How do I install the iOS / Android widget?
      - MUST give the iOS path: long-press the home/lock screen, tap the `+` icon (top-left), search for "İkra", select widget size (small / medium / large), tap "Add Widget".
      - MUST give the Android path: long-press an empty area of the home screen, choose "Widgets", scroll to "İkra", drag the desired widget size to the home screen.
      - 3-5 sentences total (can be two short paragraphs if clearer).

    FAQ Answer 2 — Audio isn't playing — what should I check?
      - MUST list, as prose or a `<ul>`: silent-mode/ringer switch on iOS; system volume + media volume on Android; Bluetooth/AirPlay routing not pointing to a disconnected device; background-app-refresh or battery-saver disabling audio playback.
      - MUST close with: if none of the above resolves it, email the support address with device model and OS version.
      - 4-6 sentences or a `<ul>` of 4-5 bullets + a closing sentence.

    FAQ Answer 3 — How do I restore my premium purchase?
      - MUST instruct: open the app → Settings (or Profile) → tap "Restore Purchase".
      - MUST state that restore requires using the same Apple ID / Google account that originally purchased the subscription.
      - MUST state that if the restore still fails, email the support address with the original purchase receipt (Apple email confirmation / Google order ID).
      - 3-4 sentences.

    FAQ Answer 4 — Daily verse notifications aren't arriving — how do I fix that?
      - MUST list checks: system Settings → Notifications → İkra → ensure "Allow Notifications" is enabled; in the app, Settings → Notifications → ensure the daily verse toggle is on and a delivery time is set; check Do Not Disturb / Focus mode is not silencing the app.
      - MUST close with: if notifications still don't arrive, email support with the device model.
      - 4-5 sentences or a `<ul>` of 3-4 bullets + a closing sentence.

    FAQ Answer 5 — How do I report a bug or request a feature?
      - MUST instruct: email the support address with a description, the steps to reproduce, device model, and OS version. Screenshots welcome.
      - MUST note feature requests are read and considered — but no promise of timeline.
      - 2-3 sentences.

    Tone notes:
      - Reverent, warm, small-team voice. Like a developer wrote it, not a corporate help desk.
      - No emoji, no exclamation marks.
      - Use `İkra` (U+0130) consistently.
      - Em dash `—` (U+2014) verbatim where it appears in the locked FAQ questions.
      - Apostrophe in `isn't` and `aren't` is a straight ASCII apostrophe `'` (NOT a curly quote) for grep-friendliness.

    Verbatim-string constraints:
      - H1: `Support`
      - Last-updated: `Last updated: June 1, 2026`
      - FAQ H2: `Frequently asked`
      - All 5 FAQ H3 question strings: VERBATIM from the table above (em dashes included)
      - Email: `support@ikraapp.com`
      - Button label: `Email us at support@ikraapp.com`
      - Cross-link labels: `Privacy Policy`, `Terms of Service`, `← Back to home`

    Do NOT use `<details>` / `<summary>` — answers must render open by default per D-24 (no JS, no client interactivity).
    Do NOT add a section background — body inherits `bg-cream` from `app/layout.tsx`.
    Do NOT add an extra `<main>` wrapper.
    Do NOT add `"use client"`.
    Do NOT add `dark:` utilities.
    Do NOT use `prose-invert` or `prose-dark`.
    Do NOT use literal `duration-150`.
    Do NOT add Open Graph or Twitter meta — Phase 4 owns that.
    Do NOT add a contact-form scaffold — D-24 explicitly uses mailto only; contact form is in CONTEXT Deferred Ideas.
    Do NOT add a screenshot, image, or icon to the page — D-24 specifies a button-link + Q+A only.
  </action>
  <verify>
    <automated>test -f app/support/page.tsx && grep -q "export const metadata" app/support/page.tsx && grep -F "Support — İkra" app/support/page.tsx && grep -F "Last updated: June 1, 2026" app/support/page.tsx && grep -F "Frequently asked" app/support/page.tsx && grep -q "mailto:support@ikraapp.com" app/support/page.tsx && grep -F "Email us at support@ikraapp.com" app/support/page.tsx && grep -F "How do I install the iOS / Android widget?" app/support/page.tsx && grep -F "Audio isn't playing — what should I check?" app/support/page.tsx && grep -F "How do I restore my premium purchase?" app/support/page.tsx && grep -F "Daily verse notifications aren't arriving — how do I fix that?" app/support/page.tsx && grep -F "How do I report a bug or request a feature?" app/support/page.tsx && grep -q "<h3>" app/support/page.tsx && grep -q "max-w-3xl" app/support/page.tsx && grep -q "px-screen" app/support/page.tsx && grep -q "py-section" app/support/page.tsx && grep -E "className=\"[^\"]*\\bprose\\b" app/support/page.tsx && grep -q "prose-headings:text-emerald-600" app/support/page.tsx && grep -q "prose-a:text-gold-500" app/support/page.tsx && grep -q "bg-emerald-600" app/support/page.tsx && grep -q "min-h-touch" app/support/page.tsx && grep -q "rounded-lg" app/support/page.tsx && grep -q "duration-fast" app/support/page.tsx && grep -q "href=\"/privacy\"" app/support/page.tsx && grep -q "href=\"/terms\"" app/support/page.tsx && grep -q "href=\"/\"" app/support/page.tsx && grep -q "border-t border-navy-800/10" app/support/page.tsx && grep -q "text-gold-500" app/support/page.tsx && ! grep -q "This page is under construction" app/support/page.tsx && ! grep -q "use client" app/support/page.tsx && ! grep -q "<details" app/support/page.tsx && ! grep -q "<summary" app/support/page.tsx && ! grep -E "dark:|prefers-color-scheme|prose-invert|prose-dark" app/support/page.tsx && ! grep -q "duration-150" app/support/page.tsx && [ $(grep -c "<h3>" app/support/page.tsx) -ge 5 ] && npm run build 2>&1 | tee /tmp/ikra-03-04-build.log | grep -E "Compiled successfully|Route \(app\)" && ! grep -E "Error|Failed to compile" /tmp/ikra-03-04-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `app/support/page.tsx` contains an `export const metadata` with `title: "Support — İkra"` and a 1-sentence `description`
    - File contains H1 string `Support` and last-updated `Last updated: June 1, 2026`
    - Primary contact button-link is an `<a href="mailto:support@ikraapp.com">` with label `Email us at support@ikraapp.com` and brand styling (bg-emerald-600, text-white, px-lg py-md, rounded-lg, min-h-touch)
    - FAQ H2 string `Frequently asked` present
    - All 5 FAQ questions present verbatim in `<h3>` elements (em dashes and ASCII apostrophes intact)
    - At least 5 `<h3>` tags present
    - No `<details>` or `<summary>` elements — answers render as plain `<p>` (D-24)
    - Body wrapper uses `prose ... max-w-3xl mx-auto px-screen py-section` with inline `prose-headings:text-emerald-600` and `prose-a:text-gold-500` overrides
    - Cross-link strip at bottom links to `/privacy`, `/terms`, `/` with gold-500 middle-dot dividers and `border-t border-navy-800/10 mt-2xl pt-lg`
    - No `"use client"`, no `dark:`, no `prefers-color-scheme`, no `prose-invert`, no `duration-150`
    - No "under construction" placeholder copy
    - `npm run build` exits 0; `/support` renders without error
  </acceptance_criteria>
  <done>`app/support/page.tsx` ships a complete Support page with the mailto button-link, the 5 locked FAQ Q+A entries rendered as `<h3>` + `<p>` (open by default for SEO), and the cross-link strip. Covers LEGAL-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static page | Pure static render; no user input, no API surface |
| visitor → mailto: link | Hands off to the OS mail client; no server-side handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Spoofing | mailto:support@ikraapp.com hardcoded | mitigate | D-25 locks this address. Developer responsibility to confirm mailbox delivery BEFORE app-store submission (CONTEXT D-25 explicitly flags this). If mailbox not yet live, override to a verified deliverable address before launch. |
| T-03-04-02 | Information Disclosure | Public exposure of support email | accept | Standard practice for support pages; the address is intentionally publishable. Mailbox owner can apply spam filtering / forwarding rules. |
| T-03-04-03 | Repudiation | FAQ answers written by Claude, not a support engineer | accept | Each answer covers basic troubleshooting that aligns with iOS/Android platform behavior; advanced cases fall through to the mailto. Acceptance criteria do not constrain answer wording beyond required content points — checker verifies presence of each required step. |
| T-03-04-04 | Denial of Service | No CAPTCHA on mailto address | accept | mailto is platform-handled; OS mail client + email provider handles abuse. No backend to protect. |
</threat_model>

<verification>
Plan-level checks:
- `/support` renders H1 + last-updated + intro + mailto button + Frequently asked H2 + 5 H3 Q + matching P answers + cross-link strip
- All 5 FAQ questions match D-24 verbatim including em dashes
- mailto button-link uses `bg-emerald-600` brand color and `min-h-touch` for tap-target compliance
- No `<details>` element (answers are always-open per D-24)
- `npm run build` exits 0; `/support` listed as static (○) route
- `git diff` shows changes scoped to `app/support/page.tsx` only
- No dark-mode utilities, no `prose-invert`, no `duration-150`
- File renders to >90 lines (substantive support page, not a stub)
</verification>

<success_criteria>
- LEGAL-03 satisfied: visitor can reach `/support` and email a human via the mailto button-link
- 5 FAQ Q+A entries cover the most common İkra app questions (widget install, audio, purchase restore, notifications, bug reports)
- Answers always open (no JS, no `<details>`) so search engines index them
- Brand-aligned typography via `prose` + inline `prose-*` overrides (D-27)
- Cross-link footer strip connects /support → /privacy, /terms, / (D-30)
- Light-mode only; no dark utilities introduced
- Build remains green
- App Store + Google Play Support URL requirement met
</success_criteria>

<output>
Create `.planning/phases/03-legal-support-pages/03-04-SUMMARY.md` documenting:
- The single content task with its commit hash
- Confirmation of mailto button-link with brand styling and `support@ikraapp.com` target
- Confirmation of all 5 FAQ questions present verbatim
- Confirmation that answers are `<h3>` + `<p>` (no `<details>`)
- Confirmation that `npm run build` exits 0 and `/support` is a static (○) route
- Decision-coverage confirmation: D-24, D-25, D-26, D-27 (use-site half), D-29, D-30 all honored, with line-number pointers
- LEGAL-03 satisfaction note
- Reminder: developer must confirm `support@ikraapp.com` mailbox delivery BEFORE app-store submission (D-25 follow-up)
- Any verbatim-copy deviations (should be zero)
</output>
</content>
</invoke>