---
phase: 04-polish-seo-launch-readiness
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - app/globals.css
  - components/Hero.tsx
  - components/Features.tsx
autonomous: false
requirements:
  - UI-03
  - UI-04
must_haves:
  truths:
    - "Hero fades in (opacity 0→1, translateY 12px→0) once on mount, ~500ms ease-out — feels quiet, not flashy (UI-04) [covers D-31]"
    - "Feature cards gain a subtle scale + gold-tinted ring on hover via CSS transition (UI-04) [covers D-31]"
    - "Users with prefers-reduced-motion: reduce see static hero (no animation) and no scale/ring transition (still brand-accurate) [covers D-32]"
    - "Visitor sees a layout that adapts cleanly across mobile (360px), tablet (768px), desktop (1024px, 1440px) with no broken or overflowing sections (UI-03) [covers D-33]"
    - "Body has overflow-x-hidden so future stray elements cannot introduce a horizontal scrollbar [covers D-35]"
    - "Lighthouse mobile run after deploy is documented in SUMMARY with scores against targets (Perf ≥ 90, A11y ≥ 95, Best Practices ≥ 95, SEO ≥ 100) — non-blocking, document only [covers D-34]"
  artifacts:
    - path: "app/globals.css"
      provides: "Body overflow-x-hidden, hero fade-in-up keyframes, feature card ring transition rule, prefers-reduced-motion override block"
      contains: "@keyframes ikra-fade-in-up"
    - path: "components/Hero.tsx"
      provides: "data-motion='hero-fade' attribute on the root section so reduced-motion selector can target it"
      contains: "data-motion"
    - path: "components/Features.tsx"
      provides: "data-motion='feature-card' attribute on each Card so reduced-motion can short-circuit scale/ring"
      contains: "data-motion"
  key_links:
    - from: "components/Hero.tsx [data-motion='hero-fade']"
      to: "app/globals.css @keyframes ikra-fade-in-up rule"
      via: "data-attribute selector binding animation"
      pattern: "data-motion=.hero-fade"
    - from: "app/globals.css @media (prefers-reduced-motion: reduce)"
      to: "[data-motion] selectors"
      via: "media query short-circuits animation/transition durations"
      pattern: "prefers-reduced-motion"
---

<objective>
Close UI-03 (responsive QA) and UI-04 (tasteful motion + polish) with CSS-only changes. Hero fades in once on load. Feature cards gain a subtle scale + gold ring on hover. Reduced-motion preference short-circuits both. Body gets `overflow-x-hidden` as a structural guarantee. Then a human responsive-QA pass across 4 viewports and an optional Lighthouse mobile run, both documented in SUMMARY.

Purpose: UI-03 + UI-04. Without this plan, the site is functional but feels flat (no motion polish) and untested on small screens.

Output: Edits to globals.css, Hero.tsx, Features.tsx; QA findings + Lighthouse scores recorded in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-seo-launch-readiness/04-CONTEXT.md
@.planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md
@app/globals.css
@components/Hero.tsx
@components/Features.tsx

<interfaces>
<!-- Current Hero root: <section className="relative overflow-hidden py-section bg-cream"> -->
<!-- Current Features card: <Card className="...transition-colors duration-fast hover:border-gold-500/30..."> -->

CSS additions go in `app/globals.css` `@layer base` (or a new `@layer utilities` block at the bottom — author decides). All animation is CSS-only — no `framer-motion`, `motion-one`, or JS-driven state per D-31.

Selectors will target `[data-motion="hero-fade"]` and `[data-motion="feature-card"]` so the reduced-motion block has stable, scope-narrow handles per D-32 (the `data-motion` attribute strategy is the cleaner of the two discretionary options).

Existing component constraints to honor:
- Hero already uses `relative overflow-hidden py-section bg-cream` — keep these classes verbatim; only add `data-motion="hero-fade"`.
- Features Card already has `transition-colors duration-fast hover:border-gold-500/30 hover:bg-surface-card-hover` — keep these; only add `data-motion="feature-card"` and reach the scale/ring via a CSS rule in globals.css (or add Tailwind utilities directly on the Card if cleaner — both valid; prefer Tailwind utilities for scale, CSS rule for the data-motion guard).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add motion CSS, reduced-motion override, and overflow-x-hidden to globals.css</name>
  <files>app/globals.css</files>
  <behavior>
    - `body` rule inside `@layer base` adds `overflow-x: hidden;` (or `@apply overflow-x-hidden`) per D-35.
    - A `@keyframes ikra-fade-in-up` rule exists at `0% { opacity: 0; transform: translateY(12px); }` → `100% { opacity: 1; transform: translateY(0); }`.
    - A rule binds `[data-motion="hero-fade"]` to `animation: ikra-fade-in-up 500ms ease-out both;` (the `both` fill-mode locks final state) per D-31.
    - A rule on `[data-motion="feature-card"]` adds `transition: transform 150ms ease-out, box-shadow 150ms ease-out, border-color 150ms ease-out;` and on `:hover` applies `transform: scale(1.01);` and a `box-shadow: 0 0 0 1px rgb(212 168 67 / 0.20);` (gold-500/20 ring per D-31).
    - A single `@media (prefers-reduced-motion: reduce)` block at the bottom resets `[data-motion="hero-fade"]` animation-duration to `0.001ms !important` + animation-iteration-count to `1 !important`, AND resets `[data-motion="feature-card"]` transition-duration to `0ms !important` and removes the hover transform (`transform: none !important` on `:hover`) per D-32.
    - Existing `transition-colors duration-fast` on Footer links / Card hover-color still works — the reduced-motion block does NOT touch generic `transition-*` utilities (per D-32 specifics: color transitions are not motion and should remain).
    - `npx next build` succeeds; no Tailwind compile errors.
  </behavior>
  <action>
    Edit `app/globals.css`. Locate the existing `@layer base { ... body { ... } }` block. Add `overflow-x-hidden` to the body apply list (e.g., `@apply bg-background text-foreground overflow-x-hidden;`) per D-35.

    Append a new section after the existing `@layer base` (use a fresh `@layer utilities` block or plain CSS at file end — choose plain CSS at file end so it has the highest specificity and isn't subject to Tailwind layer ordering surprises):

    ```css
    @keyframes ikra-fade-in-up {
      0% { opacity: 0; transform: translateY(12px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    [data-motion="hero-fade"] {
      animation: ikra-fade-in-up 500ms ease-out both;
    }

    [data-motion="feature-card"] {
      transition: transform 150ms ease-out, box-shadow 150ms ease-out, border-color 150ms ease-out;
    }
    [data-motion="feature-card"]:hover {
      transform: scale(1.01);
      box-shadow: 0 0 0 1px rgb(212 168 67 / 0.20);
    }

    @media (prefers-reduced-motion: reduce) {
      [data-motion="hero-fade"] {
        animation-duration: 0.001ms !important;
        animation-iteration-count: 1 !important;
      }
      [data-motion="feature-card"] {
        transition-duration: 0ms !important;
      }
      [data-motion="feature-card"]:hover {
        transform: none !important;
        box-shadow: none !important;
      }
    }
    ```

    Do NOT touch any color tokens or @theme block. Do NOT introduce dark-mode media queries or `dark:` utilities (D-04 forbids dark mode in v1). Do NOT install any JS animation library (D-31).
  </action>
  <verify>
    <automated>cd /Users/zbilalozgen/repos/ikra-web &amp;&amp; grep -cE "ikra-fade-in-up|prefers-reduced-motion|overflow-x-hidden|data-motion" app/globals.css | awk '$1 &gt;= 4 { print "PASS"; exit 0 } { print "FAIL: globals.css missing required motion/reduced-motion/overflow rules"; exit 1 }' &amp;&amp; ! grep -E "framer-motion|motion-one" app/globals.css &amp;&amp; npx --no-install next build 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>globals.css contains the keyframes, both data-motion rules, reduced-motion override, and body overflow-x-hidden; no JS motion lib reference; build succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire data-motion attributes on Hero and Feature cards</name>
  <files>components/Hero.tsx, components/Features.tsx</files>
  <behavior>
    - `components/Hero.tsx` root `<section>` carries `data-motion="hero-fade"`. No other JSX/structure change.
    - `components/Features.tsx` each rendered `<Card>` (inside `.map(...)`) carries `data-motion="feature-card"`. No other JSX/structure change.
    - Build succeeds; rendered HTML at `/` contains both attributes.
  </behavior>
  <action>
    Edit `components/Hero.tsx`: add `data-motion="hero-fade"` to the existing `<section className="relative overflow-hidden py-section bg-cream">` opening tag. Keep classes verbatim.

    Edit `components/Features.tsx`: inside `.map(...)`, add `data-motion="feature-card"` as a prop on the `<Card>` element (alongside the existing `className` and `key`). Do NOT modify the className string or the icon/title/description structure. Do not add inline `style` props.

    Do not introduce any React state, useEffect, or client-component directive — Hero and Features remain server components. The `data-*` attribute travels straight through to the rendered HTML without runtime cost.
  </action>
  <verify>
    <automated>cd /Users/zbilalozgen/repos/ikra-web &amp;&amp; grep -c 'data-motion=.hero-fade' components/Hero.tsx | awk '$1 &gt;= 1 { print "hero PASS" } { print }' &amp;&amp; grep -c 'data-motion=.feature-card' components/Features.tsx | awk '$1 &gt;= 1 { print "features PASS" } { print }' &amp;&amp; ! grep -E '"use client"' components/Hero.tsx components/Features.tsx &amp;&amp; npx --no-install next build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>Hero section and each Feature Card emit the correct data-motion attribute; no "use client" added; build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Hero fade-in-up + Feature card hover polish (CSS-only) + reduced-motion safety net + body overflow-x-hidden. All structural responsive guarantees in place. Ready for the human responsive QA pass at 4 viewports and the optional Lighthouse mobile run, both per D-33 and D-34.
  </what-built>
  <how-to-verify>
    1. Run `npx next dev`. Open `http://localhost:3000` in Chrome.
    2. **Motion check (normal):** Hard-refresh the page. The Hero section should fade in upward over ~500ms — quiet, not bouncy. Hover any Feature card: subtle scale-up + faint gold ring should appear.
    3. **Reduced-motion check:** macOS → System Settings → Accessibility → Display → enable "Reduce motion" (or Chrome DevTools → Rendering → Emulate CSS media feature `prefers-reduced-motion: reduce`). Hard-refresh. Hero should appear instantly (no fade). Feature card hover should have no scale/ring transition. Color-change transitions (Footer links, card border on hover) should still work.
    4. **Responsive QA at 4 viewports (D-33):** Open Chrome DevTools Device Toolbar. For each width 360px, 768px, 1024px, 1440px:
       - Scroll Home (`/`), Privacy (`/privacy`), Terms (`/terms`), Support (`/support`) end-to-end.
       - Confirm: no horizontal scrollbar at any width; no text/image overflow; all buttons remain ≥ 44px tap target on mobile; no layout shift; brand colors intact.
       - Record any issues in SUMMARY. If something breaks, the executor should fix it before resuming — common fixes are responsive Tailwind utilities (`md:`, `lg:` breakpoints) on padding/text-size, not structural rewrites.
    5. **Lighthouse mobile run (D-34, optional but document):** After deploying to Vercel (or against `npx next start` locally if Vercel deploy not ready), run `npx lighthouse https://ikra-web.vercel.app --preset=desktop --quiet --chrome-flags="--headless"` and then mobile (default). Record Performance / Accessibility / Best Practices / SEO scores in SUMMARY. Targets are aspirational, not blocking. Flag only critical regressions (missing alt text, contrast failures, CLS > 0.1).
    6. Confirm `/sitemap.xml`, `/robots.txt`, `/opengraph-image` all still respond 200 after this plan's edits.
  </how-to-verify>
  <resume-signal>
    Type "approved" with a one-line note recording: motion verified normal + reduced-motion, viewports passed (or list failures), Lighthouse scores. Or describe specific issues for fixes.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CSS rules | CSS is static; no input from user crosses to server. |
| OS preference → page rendering | `prefers-reduced-motion` is a trusted client signal honored for accessibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Denial of service | CSS animation on Hero | accept | Single 500ms animation runs once on load; trivial GPU cost. No DoS surface. |
| T-04-09 | Accessibility regression (mapped to STRIDE Tampering of user agent expectations) | Motion without reduced-motion respect | mitigate | `@media (prefers-reduced-motion: reduce)` block short-circuits animation-duration and transitions on data-motion selectors per D-32. |
| T-04-10 | Information disclosure | Lighthouse run | accept | Lighthouse runs against public production URL; no secrets exposed. Scores recorded in SUMMARY are non-sensitive. |
</threat_model>

<verification>
- Build succeeds.
- globals.css contains keyframes + reduced-motion block + overflow-x-hidden.
- Hero + each Feature card carry data-motion attribute.
- No JS animation library installed (check package.json delta).
- Human verifies motion behavior, reduced-motion behavior, and 4-viewport responsive QA at the checkpoint.
- Lighthouse scores recorded in SUMMARY (non-blocking).
</verification>

<success_criteria>
- UI-03 satisfied (4 viewports verified clean or fixes applied).
- UI-04 satisfied (motion present and tasteful; reduced-motion respected).
- D-31 through D-35 all implemented.
- No dark-mode utilities introduced.
- No JS animation library introduced.
</success_criteria>

<output>
Create `.planning/phases/04-polish-seo-launch-readiness/04-04-SUMMARY.md` when done. MUST include:
1. Viewport QA table (360 / 768 / 1024 / 1440) per page with pass/fix notes.
2. Lighthouse scores (Perf / A11y / Best Practices / SEO) — mobile, and desktop if run — against targets.
3. Any reduced-motion edge cases observed.
</output>
