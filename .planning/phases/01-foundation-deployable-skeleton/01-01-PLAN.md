---
phase: 01-foundation-deployable-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - postcss.config.mjs
  - .gitignore
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - app/icon.png
  - app/privacy/page.tsx
  - app/terms/page.tsx
  - app/support/page.tsx
  - components/Footer.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - LAND-06
tags:
  - nextjs
  - tailwindcss-v4
  - app-router
  - greenfield-scaffold

must_haves:
  truths:
    - "Visitor loads `/` locally and sees heading 'İkra — Quran Verses & Hadith' rendered in emerald-600 (#006B3F) Inter font [covers D-01, D-02 — Tailwind v4 @theme block in app/globals.css with --color-* brand tokens, no tailwind.config.js]"
    - "Visitor sees subheading 'Coming soon.' below the skeleton heading"
    - "Visitor sees a global footer on every route (/, /privacy, /terms, /support) with brand mark 'İkra', tagline 'Quran Verses & Sahih Hadith Widget App', three links labeled 'Privacy Policy', 'Terms of Service', 'Support', and copyright '© 2025 İkra. All rights reserved. [covers D-05 — footer hrefs are absolute paths /privacy /terms /support, not # placeholders]'"
    - "Visitor clicking 'Privacy Policy' navigates to /privacy and sees 'This page is under construction.' — no 404 [covers D-05 — real stub route page]"
    - "Visitor clicking 'Terms of Service' navigates to /terms and sees 'This page is under construction.' — no 404 [covers D-05]"
    - "Visitor clicking 'Support' navigates to /support and sees 'This page is under construction.' — no 404 [covers D-05]"
    - "Visitor hovering a footer link sees the color transition to gold-500 (#D4A843) over 150ms"
    - "Operator runs `npm run build` and the production build succeeds with zero errors"
    - "UI-02 (dark mode) is intentionally NOT implemented — dropped from v1 per D-04. Site renders in light mode only on every route."
  artifacts:
    - path: "app/globals.css"
      provides: "Tailwind v4 import + @theme block with brand color CSS variables (emerald, gold, navy ramps, cream, surface, dark) ported from /Users/zbilalozgen/repos/wip-muslim/tailwind.config.js"
      contains: "@import \"tailwindcss\""
      contains_also: "--color-emerald-600: #006B3F"
    - path: "app/layout.tsx"
      provides: "Root layout: <html lang='en'>, Inter font via next/font, body with cream background, renders {children} and global <Footer />"
      exports: ["default", "metadata"]
    - path: "app/page.tsx"
      provides: "Skeleton home page with heading and subheading per UI-SPEC Copywriting Contract"
      exports: ["default"]
    - path: "app/privacy/page.tsx"
      provides: "Stub Privacy route — empty-state copy 'This page is under construction.'"
      exports: ["default"]
    - path: "app/terms/page.tsx"
      provides: "Stub Terms route — empty-state copy 'This page is under construction.'"
      exports: ["default"]
    - path: "app/support/page.tsx"
      provides: "Stub Support route — empty-state copy 'This page is under construction.'"
      exports: ["default"]
    - path: "components/Footer.tsx"
      provides: "Global footer with brand mark, tagline, three nav links (Privacy/Terms/Support → absolute paths), copyright"
      exports: ["default"]
    - path: "app/icon.png"
      provides: "Favicon copied verbatim from /Users/zbilalozgen/repos/wip-muslim/src/assets/images/icon.png; App Router auto-discovers"
    - path: "package.json"
      provides: "Next.js + React + TypeScript + Tailwind v4 + ESLint dependencies, scripts: dev/build/start/lint"
      contains: "\"next\""
      contains_also: "\"tailwindcss\""
  key_links:
    - from: "app/layout.tsx"
      to: "components/Footer.tsx"
      via: "default import + render inside <body>"
      pattern: "import Footer from.*components/Footer"
    - from: "components/Footer.tsx"
      to: "/privacy, /terms, /support"
      via: "Next.js <Link> with absolute hrefs"
      pattern: "href=\"/(privacy|terms|support)\""
    - from: "app/globals.css"
      to: "Tailwind utility classes used in app/page.tsx, components/Footer.tsx"
      via: "@theme CSS variables (--color-emerald-600 etc.) enabling bg-emerald-600 / text-gold-500 / bg-cream / text-navy-800"
      pattern: "@theme"
    - from: "app/layout.tsx"
      to: "Inter font"
      via: "next/font/google import + className on <html> or <body>"
      pattern: "from \"next/font/google\""
---

<objective>
Scaffold a Next.js App Router + TypeScript + Tailwind v4 project with İkra's brand tokens, build the skeleton home page, the global Footer rendered on every route, and three stub routes (/privacy, /terms, /support) — all running locally with `npm run dev` and producing a clean `npm run build`.

This is the Walking Skeleton's vertical slice up to the point of local proof. Plan 02 takes it public on Vercel.

Purpose: Lock in the brand-accurate, route-complete shell that Phases 2–4 build on without re-litigating tokens or layout.

Output:
- A bootable Next.js project at the repo root
- Brand tokens live in `app/globals.css` `@theme`
- Skeleton `/` page renders UI-SPEC copy in emerald-600 Inter
- Global `<Footer />` on every route with three working links
- Three real stub pages — no 404s
- Production build (`npm run build`) succeeds
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-deployable-skeleton/01-UI-SPEC.md
@.planning/phases/01-foundation-deployable-skeleton/01-SKELETON.md
@CLAUDE.md

<interfaces>
<!-- Brand token source of truth — executor MUST port these values into app/globals.css @theme. -->
<!-- DO NOT invent ramps. DO NOT include dark.* tokens (D-04 drops dark mode). -->
<!-- DO NOT include darkMode config. DO NOT include @media (prefers-color-scheme) styles. -->

From /Users/zbilalozgen/repos/wip-muslim/tailwind.config.js (theme.extend.colors):

emerald: 50:#ECFDF5, 100:#D1FAE5, 200:#A7F3D0, 300:#6EE7B7, 400:#34D399, 500:#059669, 600:#006B3F, 700:#005233, 800:#003D26, 900:#002E1C
gold:    50:#FFFBEB, 100:#FEF3C7, 200:#FDE68A, 300:#FCD34D, 400:#E5C06E, 500:#D4A843, 600:#B8922E, 700:#92730A, 800:#78650D, 900:#5C4B0A
navy:    50:#EFF6FF, 100:#DBEAFE, 200:#BFDBFE, 300:#93C5FD, 400:#60A5FA, 500:#3B82F6, 600:#2D4A7A, 700:#233B63, 800:#1B2A4A, 900:#111D35
cream: #F2F2F7
surface: warm:#FFF5E6, card:#FFFFFF, card-hover:#FFFAF2

(dark.* palette intentionally omitted — D-04 drops dark mode from v1)

Spacing aliases worth porting (used downstream):
touch: 44px (footer brand mark min-height — UI-SPEC accessibility note)

Transition duration alias:
fast: 150ms (footer link hover transition — UI-SPEC Interaction Contract)

Tailwind v4 @theme syntax (executor reference — port values like `--color-emerald-600: #006B3F;` so utility classes `bg-emerald-600`, `text-emerald-600`, `border-emerald-600` etc. all resolve)
</interfaces>

<ui_spec_copy_lock>
<!-- These strings are LOCKED by 01-UI-SPEC.md Copywriting Contract. Verbatim use is required. -->

Skeleton page heading:    İkra — Quran Verses & Hadith
Skeleton page subheading: Coming soon.
Footer brand mark:        İkra
Footer tagline:           Quran Verses & Sahih Hadith Widget App
Footer Privacy link:      Privacy Policy        → href="/privacy"
Footer Terms link:        Terms of Service      → href="/terms"
Footer Support link:      Support               → href="/support"
Footer copyright:         © 2025 İkra. All rights reserved.
Empty-state stub copy:    This page is under construction.
</ui_spec_copy_lock>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bootstrap Next.js + Tailwind v4 with brand tokens in @theme</name>
  <files>package.json, tsconfig.json, next.config.ts, eslint.config.mjs, postcss.config.mjs, .gitignore, app/layout.tsx, app/page.tsx, app/globals.css, app/icon.png</files>
  <read_first>
    - /Users/zbilalozgen/repos/ikra-web/CLAUDE.md (brand hex anchors, tone)
    - /Users/zbilalozgen/repos/ikra-web/.planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md (D-01, D-02, D-04 — Tailwind v4 + @theme + drop dark mode)
    - /Users/zbilalozgen/repos/ikra-web/.planning/phases/01-foundation-deployable-skeleton/01-UI-SPEC.md (Copywriting Contract, Typography, Color light-mode rows only)
    - /Users/zbilalozgen/repos/wip-muslim/tailwind.config.js (source of truth for emerald/gold/navy/cream/surface ramps — port hex values verbatim)
    - /Users/zbilalozgen/repos/wip-muslim/src/assets/images/icon.png (favicon source)
  </read_first>
  <action>
    Initialize a Next.js App Router project at the repo root non-interactively using `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --use-npm --no-turbopack --yes`. If create-next-app installs Tailwind v3, immediately upgrade to Tailwind v4 by replacing `tailwind` and `postcss` config with the v4 setup: delete any generated `tailwind.config.js` / `tailwind.config.ts`, set `postcss.config.mjs` to use the `@tailwindcss/postcss` plugin, and install `tailwindcss@^4` + `@tailwindcss/postcss@^4` per D-01. Per D-02, do NOT create a `tailwind.config.js` — all brand tokens live in `app/globals.css` via the v4 `@theme` directive.

    Rewrite `app/globals.css` to: (a) `@import "tailwindcss";` at the top, (b) a single `@theme` block defining `--color-emerald-{50..900}`, `--color-gold-{50..900}`, `--color-navy-{50..900}`, `--color-cream`, `--color-surface-warm`, `--color-surface-card`, `--color-surface-card-hover`, `--spacing-touch: 44px`, `--transition-duration-fast: 150ms`. Use the exact hex values from `wip-muslim/tailwind.config.js` (anchors: emerald-600=#006B3F, gold-500=#D4A843, navy-800=#1B2A4A, cream=#F2F2F7). Do NOT include any `dark.*` CSS variables, do NOT add `darkMode` config, do NOT add any `@media (prefers-color-scheme: dark)` rule — UI-02 is deferred per D-04. Do NOT define `--font-family-arabic`, NativeWind-only tokens, or arabic-* font sizes — they are not used in Phase 1.

    Rewrite `app/layout.tsx` to: import Inter from `next/font/google` with `subsets: ["latin"]` and `variable: "--font-inter"`, set `<html lang="en" className={inter.variable}>`, set `<body className="bg-cream text-navy-800 font-sans antialiased min-h-screen flex flex-col">`, render `{children}` inside a `<main className="flex-1">`, and render `<Footer />` at the bottom of the body. Export a `metadata` const with `title: "İkra — Quran Verses & Hadith"` and `description: "Quran Verses & Sahih Hadith Widget App"`. Footer import comes from `components/Footer.tsx` — Task 2 creates it; Task 1 should reference it via `import Footer from "@/components/Footer"`. To keep Task 1 buildable in isolation, create a minimal placeholder `components/Footer.tsx` that exports a default function returning `<footer />` (empty); Task 2 replaces its body.

    Rewrite `app/page.tsx` to render the UI-SPEC skeleton copy inside a `<section className="flex-1 flex flex-col items-center justify-center px-screen py-section gap-md text-center">`: `<h1 className="text-2xl font-semibold leading-tight text-emerald-600">İkra — Quran Verses & Hadith</h1>` and `<p className="text-base text-navy-800/70">Coming soon.</p>`. Use exact Unicode em dash (—) and Turkish capital İ. Do NOT add any other content, links, or imagery — Phase 2 owns landing content.

    Copy `/Users/zbilalozgen/repos/wip-muslim/src/assets/images/icon.png` to `app/icon.png` so the App Router auto-discovers it as the favicon. Use `cp` — do not regenerate or modify the image.

    Ensure `.gitignore` includes `node_modules`, `.next`, `.env*.local`, `*.log` (create-next-app usually handles this). Verify `tsconfig.json` includes `"paths": {"@/*": ["./*"]}` so the `@/components/Footer` import resolves.

    Per CONTEXT.md Claude's Discretion: project layout is standard create-next-app App Router with TypeScript and ESLint on; favicon copied from wip-muslim icon.png; no `src/` directory.
  </action>
  <verify>
    <automated>cd /Users/zbilalozgen/repos/ikra-web && npm run build 2>&1 | tail -25 && grep -c '^--color-emerald-600: #006B3F' app/globals.css && grep -c '^--color-gold-500: #D4A843' app/globals.css && grep -c '^--color-navy-800: #1B2A4A' app/globals.css && grep -v '^//' app/globals.css | grep -c 'prefers-color-scheme\|darkMode\|dark:' | (read N; test "$N" = "0" && echo "DARK_MODE_ABSENT_OK" || echo "DARK_MODE_LEAK_FAIL: $N")</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists with `"next"`, `"react"`, `"react-dom"`, `"tailwindcss": "^4"`, `"@tailwindcss/postcss": "^4"`, `"typescript"`, `"eslint"` declared
    - No `tailwind.config.js` and no `tailwind.config.ts` exist anywhere in the repo (D-01)
    - `app/globals.css` starts with `@import "tailwindcss";` and contains a single `@theme { ... }` block declaring at minimum `--color-emerald-600: #006B3F;`, `--color-gold-500: #D4A843;`, `--color-navy-800: #1B2A4A;`, `--color-cream: #F2F2F7;`, plus full ramps for emerald/gold/navy 50..900 verbatim from `wip-muslim/tailwind.config.js`
    - `app/globals.css` contains ZERO occurrences of `prefers-color-scheme`, `darkMode`, or `dark:` utility class references (D-04 enforcement)
    - `app/layout.tsx` imports `Inter` from `next/font/google`, sets `<html lang="en">`, applies `bg-cream` and Inter font to `<body>`, renders `<Footer />` from `@/components/Footer`, exports `metadata` with the locked title string
    - `app/page.tsx` renders `<h1>` with exact string `İkra — Quran Verses & Hadith` (Unicode em dash U+2014, Turkish İ U+0130) using `text-emerald-600`, and `<p>` with exact string `Coming soon.`
    - `app/icon.png` exists and is byte-identical to `/Users/zbilalozgen/repos/wip-muslim/src/assets/images/icon.png` (check via `cmp app/icon.png /Users/zbilalozgen/repos/wip-muslim/src/assets/images/icon.png`)
    - `npm run build` exits 0 and produces a `.next/` directory; build log shows `/` route as static
    - `components/Footer.tsx` exists as a minimal placeholder exporting a default function (Task 2 fills it)
  </acceptance_criteria>
  <done>Project scaffolds, builds clean, brand tokens live in @theme with exact hex anchors from the app, skeleton home page renders locked copy in emerald-600 Inter, no dark-mode code anywhere.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build global Footer and three stub routes (/privacy, /terms, /support)</name>
  <files>components/Footer.tsx, app/privacy/page.tsx, app/terms/page.tsx, app/support/page.tsx</files>
  <read_first>
    - /Users/zbilalozgen/repos/ikra-web/.planning/phases/01-foundation-deployable-skeleton/01-UI-SPEC.md (Copywriting Contract, Color §light-mode tokens, Interaction Contract — Footer links + hover:text-gold-500 at 150ms; Spacing — footer column padding md/lg, vertical padding xl, brand mark min-height 44px)
    - /Users/zbilalozgen/repos/ikra-web/.planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md (D-05 — real stub routes with footer hrefs as absolute paths, not "#")
    - /Users/zbilalozgen/repos/ikra-web/app/layout.tsx (confirm Footer import path)
    - /Users/zbilalozgen/repos/ikra-web/app/globals.css (confirm available brand utility classes: bg-surface-card, text-navy-800, text-gold-500, border-navy-800)
    - /Users/zbilalozgen/repos/ikra-web/components/Footer.tsx (the placeholder from Task 1, to be replaced)
  </read_first>
  <action>
    Replace the placeholder body of `components/Footer.tsx` with the production Footer. Export a default function `Footer()` returning a semantic `<footer>` styled with `bg-surface-card text-navy-800 border-t border-navy-800/10`. Inside, render a centered max-width container `<div className="mx-auto max-w-5xl px-screen py-xl flex flex-col items-center gap-md text-center">` containing, in this exact source order:

    1. Brand mark: `<div className="font-semibold text-xl text-emerald-600 min-h-touch flex items-center">İkra</div>` (text logotype per UI-SPEC + CONTEXT.md "Footer brand mark"; `min-h-touch` enforces the 44px accessibility target).
    2. Tagline: `<p className="text-sm text-navy-800/70">Quran Verses & Sahih Hadith Widget App</p>` (exact Unicode `&` ampersand, the literal text — do not abbreviate).
    3. Nav links: a `<nav>` containing three Next.js `<Link>` elements with exact label/href pairs — `<Link href="/privacy" className="...">Privacy Policy</Link>`, `<Link href="/terms" className="...">Terms of Service</Link>`, `<Link href="/support" className="...">Support</Link>`. Each link uses className `text-sm text-navy-800 hover:text-gold-500 transition-colors duration-150` (150ms hover transition per UI-SPEC Interaction Contract). Separate links with `gap-md` on the parent `<nav className="flex flex-wrap items-center justify-center gap-md">`. hrefs are absolute paths per D-05 — never `"#"`.
    4. Copyright: `<p className="text-xs text-navy-800/60 mt-sm">© 2025 İkra. All rights reserved.</p>` (exact Unicode © U+00A9).

    Import `Link` from `next/link`. Do NOT use plain `<a>` for internal navigation. Do NOT add any social icons, language switcher, or extra columns — UI-SPEC and CONTEXT.md lock the footer composition to brand mark + tagline + three links + copyright only.

    Create three stub route pages, each as a default-exported function returning a minimal centered layout with exactly the UI-SPEC empty-state copy. Per D-05 these are real pages so footer hrefs resolve and Phase 3 replaces the body in place — do NOT redirect, do NOT render 404, do NOT use `notFound()`.

    `app/privacy/page.tsx`:
      `<section className="flex-1 flex items-center justify-center px-screen py-section text-center"><p className="text-base text-navy-800/70">This page is under construction.</p></section>`
      Export `metadata = { title: "Privacy Policy — İkra" }`.

    `app/terms/page.tsx`: identical layout, same copy, `metadata = { title: "Terms of Service — İkra" }`.

    `app/support/page.tsx`: identical layout, same copy, `metadata = { title: "Support — İkra" }`.

    Each page renders inside the root layout, so the global `<Footer />` appears automatically — do NOT import Footer in these page files.
  </action>
  <verify>
    <automated>cd /Users/zbilalozgen/repos/ikra-web && npm run build 2>&1 | tail -30 && grep -c 'href="/privacy"' components/Footer.tsx && grep -c 'href="/terms"' components/Footer.tsx && grep -c 'href="/support"' components/Footer.tsx && grep -c 'hover:text-gold-500' components/Footer.tsx && grep -c 'duration-150' components/Footer.tsx && grep -c 'min-h-touch' components/Footer.tsx && for p in app/privacy/page.tsx app/terms/page.tsx app/support/page.tsx; do grep -c 'This page is under construction.' $p; done && grep -E 'href="#"|notFound\(\)' components/Footer.tsx app/privacy/page.tsx app/terms/page.tsx app/support/page.tsx | (read R; test -z "$R" && echo "STUB_PURITY_OK" || echo "STUB_PURITY_FAIL: $R")</automated>
    <human-check>Run `npm run dev`, open http://localhost:3000, confirm: (a) heading and subheading render centered in emerald-600, (b) footer appears below with brand mark "İkra" in emerald-600, tagline, three links, and copyright, (c) clicking each footer link navigates to /privacy, /terms, /support respectively, each showing "This page is under construction." with the footer still visible, (d) hovering a footer link smoothly transitions its color to gold (#D4A843).</human-check>
  </verify>
  <acceptance_criteria>
    - `components/Footer.tsx` default-exports a `Footer` function returning a `<footer>` with exactly: text brand mark "İkra", tagline "Quran Verses & Sahih Hadith Widget App", three Next.js `<Link>` elements with hrefs `/privacy`, `/terms`, `/support` and labels "Privacy Policy", "Terms of Service", "Support", and copyright "© 2025 İkra. All rights reserved." in source order
    - Every footer `<Link>` includes `hover:text-gold-500`, `transition-colors`, and `duration-150` in its className (UI-SPEC Interaction Contract: 150ms hover to gold-500)
    - Footer brand mark element includes `min-h-touch` (44px accessibility target per UI-SPEC §Spacing Exceptions)
    - Zero occurrences of `href="#"` in `components/Footer.tsx` (D-05 — real routes, not placeholders)
    - Zero occurrences of `notFound()` in any of the three stub page files
    - `app/privacy/page.tsx`, `app/terms/page.tsx`, `app/support/page.tsx` each contain the exact string `This page is under construction.` and export a `metadata` const with the per-route title
    - `npm run build` exits 0; build log lists `/`, `/privacy`, `/terms`, `/support` as static routes
    - Visual human-check passes: footer visible on all four routes, links navigate correctly, hover transitions to gold over 150ms
  </acceptance_criteria>
  <done>Footer renders globally with locked UI-SPEC copy and gold-500 hover transition; three stub routes resolve with under-construction copy; production build covers all four routes; LAND-06 fully satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static site | Untrusted client loads static HTML/CSS/JS; no user input crosses into the app this phase |
| developer machine → npm registry | npm install pulls third-party packages (Next.js, React, Tailwind v4, ESLint, TypeScript) |
| developer machine → wip-muslim repo | Copying `icon.png` from a sibling repo — read-only file copy, no execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm dependencies installed by create-next-app | accept | Only first-party Next.js / React / Tailwind / TypeScript / ESLint packages from `npmjs.com` are installed; no exotic third-party packages added in this plan. RESEARCH.md package-legitimacy gate does not apply — no `[ASSUMED]` or `[SUS]` packages introduced. |
| T-01-02 | Information Disclosure | brand asset copy (`icon.png` from wip-muslim) | accept | `icon.png` is the public app icon; no PII or secrets. Read-only copy via `cp`. |
| T-01-03 | Denial of Service | static skeleton page | accept | Site is fully static (SSG); no runtime state, no API, no DB. DoS surface is Vercel's static-asset CDN, not application code. |
| T-01-04 | Spoofing | footer link navigation | mitigate | Footer links use Next.js `<Link>` with hardcoded absolute paths `/privacy`, `/terms`, `/support` — no user-controlled URLs, no `href="#"` placeholders that could be repurposed. |
</threat_model>

<verification>
- `npm run build` succeeds with zero errors
- All four routes (`/`, `/privacy`, `/terms`, `/support`) appear as static in build output
- `app/globals.css` contains brand `@theme` block with exact emerald-600/gold-500/navy-800/cream anchors and full ramps from `wip-muslim/tailwind.config.js`
- `app/globals.css` contains ZERO dark-mode artifacts (`prefers-color-scheme`, `darkMode`, `dark:` utilities) — D-04 enforcement
- Footer renders globally on every route with locked UI-SPEC copy and absolute-path hrefs
- Footer link hover transitions to gold-500 over exactly 150ms per UI-SPEC Interaction Contract
- Skeleton home page heading uses exact Unicode em dash and Turkish İ
- Favicon `app/icon.png` is byte-identical to `wip-muslim/src/assets/images/icon.png`
</verification>

<success_criteria>
- UI-01 satisfied: emerald-600 / gold-500 / navy-800 / cream tokens applied via Tailwind utility classes whose names match the mobile app
- UI-02 explicitly deferred per D-04: zero dark-mode code in this plan, documented in must_haves.truths
- LAND-06 satisfied: global footer with brand mark "İkra" and three working links to Privacy / Terms / Support stub routes
- Local `npm run dev` serves all four routes; local `npm run build` produces a clean production build
- Plan 02 inherits a deployable repo ready for git remote + Vercel connection
</success_criteria>

<output>
Create `.planning/phases/01-foundation-deployable-skeleton/01-01-SUMMARY.md` when done. Include: files created, brand tokens ported (with hex anchors), routes shipped, build verification output, and confirmation that UI-02 is intentionally absent per D-04.
</output>
