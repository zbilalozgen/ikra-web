---
phase: 04-polish-seo-launch-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/sitemap.ts
  - app/robots.ts
autonomous: true
requirements:
  - SEO-03
must_haves:
  truths:
    - "Site serves a valid sitemap.xml listing /, /privacy, /terms, /support (SEO-03) [covers D-40]"
    - "Site serves a valid robots.txt that allows all crawlers and points at the sitemap (SEO-03) [covers D-41]"
    - "robots.txt does NOT disallow any path — pre-launch site needs indexing (specifics in CONTEXT)"
  artifacts:
    - path: "app/sitemap.ts"
      provides: "Next.js sitemap route returning MetadataRoute.Sitemap with 4 URLs"
      exports: ["default"]
    - path: "app/robots.ts"
      provides: "Next.js robots route returning MetadataRoute.Robots allowing all + sitemap pointer"
      exports: ["default"]
  key_links:
    - from: "app/robots.ts sitemap field"
      to: "app/sitemap.ts (/sitemap.xml)"
      via: "string interpolation of siteUrl + /sitemap.xml"
      pattern: "sitemap.*xml"
    - from: "app/sitemap.ts URL absolute resolution"
      to: "process.env.NEXT_PUBLIC_SITE_URL"
      via: "string interpolation with fallback"
      pattern: "NEXT_PUBLIC_SITE_URL"
---

<objective>
Create the two Next.js metadata route files that complete SEO-03: `app/sitemap.ts` (returns the 4-URL sitemap.xml) and `app/robots.ts` (returns robots.txt allowing all crawlers and pointing at the sitemap).

Purpose: SEO-03. Without these files, the site has no sitemap.xml or robots.txt — Google Search Console flags it, and indexing relies on link discovery alone.

Output: Two new files. No edits to existing code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-seo-launch-readiness/04-CONTEXT.md

<interfaces>
<!-- Next.js 16 metadata file conventions. -->

```typescript
// app/sitemap.ts
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    { url: "https://...", lastModified: new Date(), changeFrequency: "monthly", priority: 1.0 },
    ...
  ];
}
```

```typescript
// app/robots.ts
import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [{ userAgent: "*", allow: "/" }],
    sitemap: "https://.../sitemap.xml",
  };
}
```

Routes:
- /             — priority 1.0 (D-40)
- /privacy      — priority 0.7 (D-40)
- /terms        — priority 0.7 (D-40)
- /support      — priority 0.7 (D-40)
- changeFrequency: "monthly" for all (D-40)
- lastModified: new Date() (build time) (D-40)

Env var: `process.env.NEXT_PUBLIC_SITE_URL ?? "https://ikra-web.vercel.app"` (matches 04-01 fallback exactly).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create app/sitemap.ts emitting 4 URLs</name>
  <files>app/sitemap.ts</files>
  <behavior>
    - File default-exports a function returning `MetadataRoute.Sitemap` with exactly 4 entries.
    - Each entry's `url` is `${siteUrl}${path}` where siteUrl = `process.env.NEXT_PUBLIC_SITE_URL ?? "https://ikra-web.vercel.app"` and path = `/`, `/privacy`, `/terms`, `/support`.
    - `/` has `priority: 1.0`; `/privacy`, `/terms`, `/support` each have `priority: 0.7`.
    - Every entry has `changeFrequency: "monthly"` and `lastModified: new Date()`.
    - `npx next build` succeeds and the build output lists `/sitemap.xml` as a route.
  </behavior>
  <action>
    Create `app/sitemap.ts`. Import `MetadataRoute` type from `next`. Declare `const siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? "https://ikra-web.vercel.app";` at module top. Default-export `function sitemap(): MetadataRoute.Sitemap { ... }` returning the 4-entry array per D-40 with exact priorities, `changeFrequency: "monthly"`, and `lastModified: new Date()`.

    Use the exact fallback string `"https://ikra-web.vercel.app"` — must match 04-01 byte-for-byte so canonical/sitemap stay consistent. Do not introduce any other URLs (e.g., favicon.ico is excluded; sitemap is for indexable HTML pages only).
  </action>
  <verify>
    <automated>cd /Users/zbilalozgen/repos/ikra-web &amp;&amp; grep -cE "/privacy|/terms|/support|priority.*1" app/sitemap.ts | awk '$1 &gt;= 4 { print "PASS"; exit 0 } { print "FAIL: sitemap missing required routes/priority"; exit 1 }' &amp;&amp; npx --no-install next build 2&gt;&amp;1 | grep -E "sitemap\\.xml|sitemap" | head -5</automated>
  </verify>
  <done>app/sitemap.ts default-exports the 4-URL sitemap; build lists `/sitemap.xml` route; `curl -s http://localhost:3000/sitemap.xml` (after `npx next start`) returns XML containing all 4 URLs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create app/robots.ts allowing all + sitemap pointer</name>
  <files>app/robots.ts</files>
  <behavior>
    - File default-exports a function returning `MetadataRoute.Robots`.
    - Returned object has `rules: [{ userAgent: "*", allow: "/" }]` (single rule, no disallow — per CONTEXT specifics: pre-launch site needs indexing).
    - Returned object has `sitemap: "${siteUrl}/sitemap.xml"` using the same siteUrl resolution as sitemap.ts.
    - `npx next build` succeeds and the build output lists `/robots.txt` as a route.
    - At runtime, `curl -s http://localhost:3000/robots.txt` returns:
      ```
      User-Agent: *
      Allow: /
      Sitemap: https://ikra-web.vercel.app/sitemap.xml
      ```
      (or with the NEXT_PUBLIC_SITE_URL value if set).
  </behavior>
  <action>
    Create `app/robots.ts`. Import `MetadataRoute` type from `next`. Declare `const siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? "https://ikra-web.vercel.app";` (same as sitemap.ts — repeat verbatim, do not extract to a shared util; two-file duplication is cheaper than wiring a lib for v1). Default-export `function robots(): MetadataRoute.Robots { return { rules: [{ userAgent: "*", allow: "/" }], sitemap: `${siteUrl}/sitemap.xml` }; }` per D-41.

    Do NOT add any `disallow` entries (CONTEXT.md specifics: pre-launch indexing required). Do NOT add multiple userAgent rules (no Googlebot-specific carve-outs for v1).
  </action>
  <verify>
    <automated>cd /Users/zbilalozgen/repos/ikra-web &amp;&amp; grep -E 'userAgent.*\*|allow.*/|sitemap.*xml' app/robots.ts | grep -v '^//' | wc -l | awk '$1 &gt;= 3 { print "PASS"; exit 0 } { print "FAIL: robots.ts missing required fields"; exit 1 }' &amp;&amp; ! grep -q "disallow" app/robots.ts &amp;&amp; npx --no-install next build 2&gt;&amp;1 | grep -E "robots\\.txt|robots" | head -5</automated>
  </verify>
  <done>app/robots.ts emits the expected robots.txt with allow-all + sitemap pointer; build lists `/robots.txt` route; no `disallow` directive present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler → sitemap/robots | Untrusted crawlers fetch these endpoints; read-only static-ish responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information disclosure | sitemap.xml URL list | accept | All 4 URLs are already-public landing/legal pages; sitemap intentionally advertises them. |
| T-04-07 | Tampering | robots.txt allow-all | accept | Pre-launch site intentionally allows all crawlers. Operator can tighten before launch if needed; CONTEXT specifics flag this as a required behavior. |
</threat_model>

<verification>
- `npx next build` succeeds.
- Build output lists `/sitemap.xml` and `/robots.txt` routes.
- `curl -s http://localhost:3000/sitemap.xml` returns valid XML with 4 `<url>` entries (4 `<loc>` tags).
- `curl -s http://localhost:3000/robots.txt` returns allow-all + sitemap line, no disallow.
</verification>

<success_criteria>
- SEO-03 satisfied.
- robots.txt does not disallow anything.
- Sitemap lists exactly 4 URLs with correct priorities.
</success_criteria>

<output>
Create `.planning/phases/04-polish-seo-launch-readiness/04-03-SUMMARY.md` when done.
</output>
